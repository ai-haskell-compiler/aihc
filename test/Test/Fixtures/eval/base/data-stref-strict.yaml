extensions:
  - MagicHash
modules:
  - |
    module Test where

    import Data.STRef (modifySTRef', newSTRef)
    import GHC.Prim (raise#)
    import GHC.ST (runST)

    strictModification :: Bool
    strictModification = runST (do
      reference <- newSTRef True
      modifySTRef' reference (\_ -> raise# False)
      return True)
output: ""
expression: strictModification
error: 'uncaught exception: False'
status: fail
reason: modifySTRef' forces an updated value to weak head normal form before returning
