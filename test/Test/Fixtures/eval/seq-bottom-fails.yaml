extensions:
  - MagicHash
modules:
  - |
    module Test where

    import GHC.Prim (raise#)

    data Unit = Unit

    raisedUnit :: Unit
    raisedUnit = raise# Unit

    result :: Bool
    result = seq raisedUnit True
output: ""
exception: Unit
expression: result
status: pass
reason: seq raises the first argument's exact Unit exception while evaluating it to weak head normal form
