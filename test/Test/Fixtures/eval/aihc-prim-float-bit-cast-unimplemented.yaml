dependencies:
  - aihc-base
  - aihc-prim
extensions:
  - MagicHash
modules:
  - |
    module Test where

    import GHC.Int (Int (..))
    import GHC.Prim
output: |
  I# 1065353216
expression: |
  I# (word2Int# (word32ToWord# (castFloatToWord32# (int2Float# 1#))))
status: xfail
reason: the IEEE 754 bit-pattern casts have declarations only, thus no backend gives them a runtime yet
