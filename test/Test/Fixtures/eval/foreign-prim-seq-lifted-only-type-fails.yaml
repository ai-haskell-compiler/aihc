extensions:
  - GHCForeignImportPrim
modules:
  - |
    module Test where

    foreign import prim seq :: a -> b -> b
output: ""
expression: seq
status: fail
reason: seq requires GHC's representation-polymorphic scheme, not only its lifted specialization
