extensions: []
modules:
  - |
    module Test where
    a + b = (a,b)
    a * b = (a,b)
    infixl 6 +
    infixl 7 *
output: |
  ('a',('b','c'))
expression: |
  'a' + 'b' * 'c'
status: pass
reason: groups higher-precedence right operand before lower-precedence operator
