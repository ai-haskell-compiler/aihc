extensions:
  - CApiFFI
  - ForeignFunctionInterface
modules:
  - |
    module Test where
    foreign import capi "limits.h value INT_MAX" intMax :: Int
output: ""
expression: |
  intMax
error: 'typecheck error: Test: a capi value import is not supported: limits.h value INT_MAX'
status: fail
reason: a capi value entity reads a C constant through the header, which this compiler cannot do
