extensions:
  - MagicHash
modules:
  - |
    module Test where
    loop :: () -> Int#
    loop value = loop value
    constInt :: Int# -> Int# -> Int#
    constInt value _ = value
output: |
  42
expression: |
  constInt 42# (loop ())
status: pass
reason: preserves non-strict application through explicit GRIN thunks
