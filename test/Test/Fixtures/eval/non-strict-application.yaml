extensions:
  - MagicHash
modules:
  - |
    module Test where
    data Int = I# Int#
    loop :: () -> Int
    loop value = loop value
    constInt :: Int -> Int -> Int
    constInt value _ = value
output: |
  I# 42
expression: |
  constInt (I# 42#) (loop ())
status: pass
reason: preserves non-strict application for lifted arguments through explicit GRIN thunks
