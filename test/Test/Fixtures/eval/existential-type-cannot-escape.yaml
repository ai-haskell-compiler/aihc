extensions:
  - ExistentialQuantification
modules:
  - |
    {-# LANGUAGE ExistentialQuantification #-}
    module Test where

    data Box = forall a. Box a

    escape (Box value) = value
output: ""
expression: |
  escape
error: 'typecheck error: Test: internal type checker abort: internal type annotation error: unzonked meta-variable in finalized type annotation'
status: fail
reason: a constructor-local existential type must not escape its pattern-match branch
