extensions:
  - CApiFFI
  - ForeignFunctionInterface
modules:
  - |
    module Test where

    foreign import capi "limits.h value INT_MAX" intMax :: Int
output: |
  I# 2147483647
expression: intMax
status: pass
reason: a capi value import reads a C constant through the header, which the generated C wrapper returns
