extensions:
  - ExtendedLiterals
  - ForeignFunctionInterface
  - MagicHash
modules:
  - |
    module Test where

    import Data.Word (Word32 (..))
    import Foreign.C.Types (CUInt (..))

    foreign import ccall unsafe "srand" c_srand :: CUInt -> IO ()
output: |
  ()
expression: |
  c_srand (CUInt (W32# 7#Word32))
status: pass
reason: a C procedure with a unit result binds no value and gives the unit constructor
