extensions:
  - ExtendedLiterals
  - ForeignFunctionInterface
  - MagicHash
modules:
  - |
    module Test where

    import Data.Int (Int64 (..))
    import Foreign.C.Types (CLLong (..))

    foreign import ccall unsafe "llabs" c_llabs :: CLLong -> CLLong
output: |
  I64# 42
expression: |
  c_llabs (CLLong (I64# (-42#Int64)))
status: pass
reason: a CLLong marshals through its newtype and Int64 as a 64-bit signed C value in both directions
