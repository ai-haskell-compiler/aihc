name: Hackage Install

on:
  schedule:
    - cron: "41 2 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

concurrency:
  group: hackage-install
  cancel-in-progress: false

jobs:
  select-runner:
    runs-on: ubuntu-24.04
    outputs:
      use_self_hosted: ${{ steps.select.outputs.use_self_hosted }}
      runs_on: ${{ steps.select.outputs.runs_on }}
      selected_runner: ${{ steps.select.outputs.selected_runner }}
      reason: ${{ steps.select.outputs.reason }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - id: select
        uses: ./.github/actions/select-ci-runner
        with:
          repository: ${{ github.repository }}
          runner-label: ${{ vars.CI_SELF_HOSTED_RUNNER_LABEL != '' && vars.CI_SELF_HOSTED_RUNNER_LABEL || 'aihc-lima' }}
          fallback-runner: ${{ vars.CI_HOSTED_FALLBACK_RUNNER != '' && vars.CI_HOSTED_FALLBACK_RUNNER || 'ubuntu-24.04' }}
          runner-token: ${{ secrets.SELF_HOSTED_RUNNER_ADMIN_TOKEN != '' && secrets.SELF_HOSTED_RUNNER_ADMIN_TOKEN || secrets.AUTOMATION_PR_TOKEN }}
          allow-self-hosted: true

      - name: Summarize runner selection
        run: |
          {
            echo "### Runner selection"
            echo
            echo "- Selected runner: \`${{ steps.select.outputs.selected_runner }}\`"
            echo "- Self-hosted enabled: \`${{ steps.select.outputs.use_self_hosted }}\`"
            echo "- Reason: ${{ steps.select.outputs.reason }}"
          } >>"$GITHUB_STEP_SUMMARY"

  install-hackage-packages:
    needs: select-runner
    runs-on: ${{ fromJSON(needs.select-runner.outputs.runs_on) }}
    env:
      XDG_CACHE_HOME: ${{ github.workspace }}/.cache
      REPORT_DIR: ${{ github.workspace }}/hackage-install-report
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Nix CI
        uses: ./.github/actions/setup-nix-ci
        with:
          r2-account-id: ${{ vars.R2_ACCOUNT_ID }}
          r2-access-key: ${{ vars.R2_ACCESS_KEY }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          nix-cache-private-key: ${{ secrets.NIX_CACHE_PRIVATE_KEY }}

      - name: Install the pinned Hackage packages
        id: install
        continue-on-error: true
        run: nix run .#install-hackage-packages -- --report-dir "$REPORT_DIR"

      - name: Summarize the run
        run: |
          set -euo pipefail
          {
            echo "### Hackage install"
            echo
            if [ -s "$REPORT_DIR/failed.txt" ]; then
              echo "Failed to install:"
              echo
              while read -r package; do
                echo "- \`$package\`"
              done <"$REPORT_DIR/failed.txt"
            else
              echo "Every package of \`docs/hackage-install-packages.md\` installed."
            fi
          } >>"$GITHUB_STEP_SUMMARY"

      - name: Open an issue for each failed package
        if: steps.install.outcome == 'failure'
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail

          label="hackage-install-failure"
          gh label create "$label" \
            --description "A package of docs/hackage-install-packages.md fails to install" \
            --color B60205 >/dev/null 2>&1 || true

          if [ ! -s "$REPORT_DIR/failed.txt" ]; then
            echo "The install step failed before any package was attempted." >&2
            exit 1
          fi

          # One issue per package, titled after the exact release, so a rerun
          # that still fails finds its own issue rather than opening another.
          open_titles="$(gh issue list --state open --label "$label" --limit 200 --json title --jq '.[].title')"

          while read -r package; do
            title="aihc install fails: $package"
            if printf '%s\n' "$open_titles" | grep -Fxq "$title"; then
              echo "An open issue already tracks $package."
              continue
            fi
            body="$REPORT_DIR/body-$package.md"
            {
              cat "$REPORT_DIR/$package.md"
              echo
              echo "Found by the daily [Hackage install]($RUN_URL) workflow."
            } >"$body"
            gh issue create --title "$title" --label "$label" --body-file "$body"
          done <"$REPORT_DIR/failed.txt"

      - name: Fail when a package failed to install
        if: steps.install.outcome == 'failure'
        run: exit 1
