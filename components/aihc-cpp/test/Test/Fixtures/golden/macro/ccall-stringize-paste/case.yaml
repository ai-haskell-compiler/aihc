input: |-
  #define CCALL(name,signature) \
  foreign import ccall unsafe #name \
      c_##name :: signature

  CCALL(foo, Int -> IO ())
output: |-
  #line 1 "input.hs"

  foreign import ccall unsafe "foo"     c_foo :: Int -> IO ()
status: xfail
reason: stringification and token pasting are not implemented yet
