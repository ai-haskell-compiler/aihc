extensions: [UnboxedTuples]
input: |
  {-# LANGUAGE UnboxedTuples #-}
  module UnboxedTuplesSingleton where
  x = (# 1 #)
  f (# y #) = y
ast: "Module {ModuleHead {\"UnboxedTuplesSingleton\"}, [EnableExtension UnboxedTuples], [DeclValue (PatternBind (PVar \"x\") (ETuple Unboxed [Just (EInt 1 TInteger)])), DeclValue (FunctionBind UnqualifiedName {\"f\"} [Match {MatchHeadPrefix, [PTuple Unboxed [PVar \"y\"]], EVar Name {\"y\"}}])]}"
status: pass
