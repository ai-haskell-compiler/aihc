extensions: [DataKinds, FlexibleInstances, TypeOperators]
input: |
  {-# LANGUAGE DataKinds #-}
  {-# LANGUAGE FlexibleInstances #-}
  {-# LANGUAGE TypeOperators #-}
  module M where
  class Infix a b
  instance '() `Infix` ()
ast: "Module {ModuleHead {\"M\"}, [EnableExtension DataKinds, EnableExtension FlexibleInstances, EnableExtension TypeOperators], [DeclClass (ClassDecl {PrefixBinderHead (UnqualifiedName {\"Infix\"}) [TyVarBinder {\"a\"}, TyVarBinder {\"b\"}]}), DeclInstance (InstanceDecl {TInfix (TTuple Boxed Promoted []) Name {\"Infix\"} (TTuple Boxed Unpromoted [])})]}"
status: pass
