extensions: [MultilineStrings]
input: |
  """
      indented
      text
      """
ast: EString "indented\ntext"
status: pass
comment: |
  Common indentation (6 spaces) is stripped from all lines.
  First newline after """ is removed (step 6).
  Last newline before """ is removed (step 7).
  Whitespace-only lines are completely removed.
