extensions: []
modules:
  - |
    module Test where
    f $ x = f x
expected:
  - "($) :: (a -> b) -> a -> b"
status: xfail
reason: "tcMatches ignores function argument patterns, so `f` and `x` in the RHS are unbound variables"
