extensions: []
modules:
  - |
    module Test where
    id = \x -> x
expected:
  - "id :: forall a. a -> a"
status: xfail
reason: "inferLambda does not bind pattern variables into the environment, so `x` in the body is unbound"
