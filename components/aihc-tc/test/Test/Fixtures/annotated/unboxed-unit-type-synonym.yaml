annotated:
- |-
  module Test where
  type Nil = (# #)
       └─ type: TYPE ('TupleRep '[])
extensions:
- UnboxedTuples
modules:
- |
  module Test where
  type Nil = (# #)
reason: infers TYPE (TupleRep []) for an unboxed unit type synonym
status: pass
