annotated:
- |-
  module Test where
  f x = let g y = h y; h z = z in g x
  │ └─ x ∷ a│ └─ y ∷ a │ └─ z ∷ a
  │         │          └─ h ∷ ∀ a. a → a
  │         └─ g ∷ ∀ a. a → a
  └─ f ∷ ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g y = h y; h z = z in g x
reason: ''
status: pass
