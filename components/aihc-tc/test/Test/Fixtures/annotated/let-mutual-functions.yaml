annotated:
- |-
  module Test where
  f x = let g y = h y; h z = z in g x
  │ │ │     │ └─ y ∷ a │ └─ z ∷ a
  │ │ │     │          └─ h ∷ ∀ a. a → a
  │ │ │     └─ g ∷ ∀ a. a → a
  │ │ └─ type-args: a
  │ └─ x ∷ a
  └─ f ∷ ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g y = h y; h z = z in g x
reason: ''
status: pass
