annotated:
- |-
  module Test where
  f x = let g y = h y; h z = z in g x
  │ └─ x ∷ a│ │        │ │        └─ type: a → a; type-args: a
  │         │ │        │ └─ z ∷ ?12
  │         │ │        └─ type: ∀ a. a → a
  │         │ └─ y ∷ ?12
  │         └─ type: ∀ a. a → a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g y = h y; h z = z in g x
reason: ''
status: pass
