annotated:
- |-
  module Test where
  f x = let g y = h y; h z = z in g x
  │         │     │ │  │     │    │ └─ type: a
  │         │     │ │  │     │    └─ type: a → a; type-args: a
  │         │     │ │  │     └─ type: ?12
  │         │     │ │  └─ type: ∀ a. a → a
  │         │     │ └─ type: ?12
  │         │     └─ type: ?12 → ?12
  │         └─ type: ∀ a. a → a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g y = h y; h z = z in g x
reason: ''
status: pass
