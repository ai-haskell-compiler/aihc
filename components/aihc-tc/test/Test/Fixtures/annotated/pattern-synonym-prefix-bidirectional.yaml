annotated:
- |-
  {-# LANGUAGE PatternSynonyms #-}
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: Type
  data Expr = Lit Bool | Add Expr Expr | Neg Expr
       │      │          │               └─ type: Expr → Expr
       │      │          └─ type: Expr → Expr → Expr
       │      └─ type: Bool → Expr
       └─ type: Type
  pattern Sub a b = Add a (Neg b)
  │                 │   ││  │   └─ type: Expr
  │                 │   ││  │   └─ type: Expr
  │                 │   ││  └─ type: Expr
  │                 │   └─ type: Expr
  │                 │   └─ type: Expr
  │                 └─ type: Expr
  └─ type: Expr → Expr → Expr
  swap :: Expr -> Expr
  swap (Sub a b) = Sub b a
  │     │   │ └─ type: Expr
  │     │   └─ type: Expr
  │     └─ type: Expr
  └─ type: Expr → Expr
  swap e = e
  │    └─ type: Expr
  └─ type: Expr → Expr

extensions:
- PatternSynonyms
modules:
- |
  {-# LANGUAGE PatternSynonyms #-}
  module Test where
  data Bool = True | False
  data Expr = Lit Bool | Add Expr Expr | Neg Expr
  pattern Sub a b = Add a (Neg b)
  swap :: Expr -> Expr
  swap (Sub a b) = Sub b a
  swap e = e
reason: a bidirectional pattern synonym gets a constructor-like type, and its matcher and builder are checked
status: pass
