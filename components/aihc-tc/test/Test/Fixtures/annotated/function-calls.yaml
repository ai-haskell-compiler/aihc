annotated:
- |-
  module Test where
  f x = x
  │ └─ type: a
  └─ type: ∀ a. a → a
  g y = f (f y)
  │ │   │  └─ type: a → a; type-args: a
  │ │   └─ type: a → a; type-args: a
  │ └─ type: a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = x
  g y = f (f y)
reason: ''
status: pass
