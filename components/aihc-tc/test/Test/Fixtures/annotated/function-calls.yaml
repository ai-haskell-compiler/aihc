annotated:
- |-
  module Test where
  f x = x
  │ └─ x :: a
  └─ f :: forall a. a -> a
  g y = f (f y)
  │ │   │  └─ type-args: a
  │ │   └─ type-args: a
  │ └─ y :: a
  └─ g :: forall a. a -> a
extensions: []
modules:
- |
  module Test where
  f x = x
  g y = f (f y)
reason: ''
status: pass
