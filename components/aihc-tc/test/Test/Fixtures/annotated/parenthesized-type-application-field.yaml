annotated:
- |-
  module Test where
  data Int = I
  └─ type: * └─ type: Int
  data Foo a = Foo
  │            └─ type: Foo a
  └─ type: * → *
  data Chan a = Chan (Foo Int)
  │             └─ type: Foo Int → Chan a
  └─ type: * → *
extensions: []
modules:
- |
  module Test where
  data Int = I
  data Foo a = Foo
  data Chan a = Chan (Foo Int)
reason: ''
status: pass
