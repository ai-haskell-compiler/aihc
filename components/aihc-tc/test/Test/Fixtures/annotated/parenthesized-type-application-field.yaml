annotated:
- |-
  module Test where
  data Int = I
       │     └─ type: Int
       └─ type: *
  data Foo a = Foo
       │       └─ type: ∀ a. Foo a
       └─ type: * → *
  data Chan a = Chan (Foo Int)
       │        └─ type: ∀ a. Foo Int → Chan a
       └─ type: * → *
extensions: []
modules:
- |
  module Test where
  data Int = I
  data Foo a = Foo
  data Chan a = Chan (Foo Int)
reason: ''
status: pass
