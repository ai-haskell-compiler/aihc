annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: *
  f x = let y = x + one in y
  │ └─ x ∷ a│     └─ type: a → Bool → a; type-args: a, Bool
  │         └─ type: a
  └─ type: ∀ a. a → a
  (+) a b = a
   │  │ └─ b ∷ b
   │  └─ a ∷ a
   └─ type: ∀ a b. a → b → a
  one = True
  └─ type: Bool
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x = let y = x + one in y
  (+) a b = a
  one = True
reason: ''
status: pass
