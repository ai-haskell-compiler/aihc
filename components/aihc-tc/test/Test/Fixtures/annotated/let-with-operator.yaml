annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ False :: Bool
       │      └─ True :: Bool
       └─ Bool :: *
  f x = let y = x + one in y
  │ │       │ └─ type-args: a, Bool
  │ │       └─ y :: a
  │ └─ x :: a
  └─ f :: forall a. a -> a
  (+) a b = a
   │  │ └─ b :: b
   │  └─ a :: a
   └─ (+) :: forall a b. a -> b -> a
  one = True
  └─ one :: Bool
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x = let y = x + one in y
  (+) a b = a
  one = True
reason: ''
status: pass
