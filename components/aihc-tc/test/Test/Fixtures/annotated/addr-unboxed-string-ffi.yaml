annotated:
- |-
  module Test where

  import GHC.Prim (Addr#, Int32#)

  data Int32 = I32# Int32#
       │       └─ type: GHC.Prim.Int32# → Int32
       └─ type: Type
  newtype CInt = CInt Int32
          │      └─ type: Int32 → CInt
          └─ type: Type
  data IO a
       └─ type: Type → Type

  foreign import ccall unsafe puts :: Addr# -> IO CInt
  └─ type: GHC.Prim.Addr# → IO CInt

  main :: IO CInt
  main = puts "hello world\n"#
  │           └─ type: GHC.Prim.Addr#
  └─ type: IO CInt
extensions:
- ForeignFunctionInterface
- MagicHash
modules:
- |
  module Test where

  import GHC.Prim (Addr#, Int32#)

  data Int32 = I32# Int32#
  newtype CInt = CInt Int32
  data IO a

  foreign import ccall unsafe puts :: Addr# -> IO CInt

  main :: IO CInt
  main = puts "hello world\n"#
reason: infers unboxed strings as Addr# and accepts Addr# as an unsafe ccall argument
status: pass
