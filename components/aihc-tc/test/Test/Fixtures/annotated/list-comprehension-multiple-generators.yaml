annotated:
- |-
  module Test where
  pairs xs ys = [ (x, y) | x <- xs, y <- ys ]
  │     │  │       ││  │    │    │   │    └─ type: [b]
  │     │  │       ││  │    │    │   └─ y ∷ b
  │     │  │       ││  │    │    └─ type: [a]
  │     │  │       ││  │    └─ x ∷ a
  │     │  │       ││  └─ type: b
  │     │  │       └─ type: a
  │     │  │       └─ type: (a, b); type-args: a, b
  │     │  └─ ys ∷ [b]
  │     └─ xs ∷ [a]
  └─ type: ∀ a b. [a] → [b] → [(a, b)]
extensions: []
modules:
- |
  module Test where
  pairs xs ys = [ (x, y) | x <- xs, y <- ys ]
reason: later generators can see earlier generator bindings and produce tuple elements
status: pass
