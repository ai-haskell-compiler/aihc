annotated:
- |-
  module Test where

  import GHC.Prim (Addr#, Int32#)

  data CInt = CInt Int32#
       │      └─ type: GHC.Prim.Int32# → CInt
       └─ type: Type

  foreign import capi unsafe "demo.h value DEMO" demoValue :: CInt
  └─ type: CInt

  foreign import capi unsafe "demo.h demo_call" demoCall :: CInt -> CInt
  └─ type: CInt → CInt

  foreign import capi unsafe "demo_plain" demoPlain :: CInt -> CInt
  └─ type: CInt → CInt

  foreign import capi unsafe "demo.h &demo_data" demoData :: Addr#
  └─ type: GHC.Prim.Addr#
extensions:
- CApiFFI
- MagicHash
modules:
- |
  module Test where

  import GHC.Prim (Addr#, Int32#)

  data CInt = CInt Int32#

  foreign import capi unsafe "demo.h value DEMO" demoValue :: CInt

  foreign import capi unsafe "demo.h demo_call" demoCall :: CInt -> CInt

  foreign import capi unsafe "demo_plain" demoPlain :: CInt -> CInt

  foreign import capi unsafe "demo.h &demo_data" demoData :: Addr#
reason: a capi entity names a header, an optional value or address mark, and the C entity
status: pass
