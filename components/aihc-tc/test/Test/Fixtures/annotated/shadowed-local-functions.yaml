annotated:
- |-
  module Test where
  x x = let x x = x in x where x x = x
  │ └─ x ∷ a│ │   │    │       │ │   └─ type: ?5
  │         │ │   │    │       │ └─ x ∷ ?5
  │         │ │   │    │       └─ type: ∀ a. a → a
  │         │ │   │    └─ type: b → b; type-args: b
  │         │ │   └─ type: ?11
  │         │ └─ x ∷ ?11
  │         └─ type: ∀ a. a → a
  └─ type: ∀ a b. a → b → b
extensions: []
modules:
- |
  module Test where
  x x = let x x = x in x where x x = x
reason: local annotation uses recorded binder elaborations, not textual scope reconstruction
status: pass
