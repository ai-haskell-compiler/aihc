annotated:
- |-
  {-# LANGUAGE MagicHash #-}
  module Main where
  import GHC.Classes (Eq, Ord)
  import GHC.Prim (Addr#)
  data T = T Addr#
  │    │   └─ type: GHC.Prim.Addr# → T
  │    └─ type: Type
  └─ deriving plans: stock Eq T [context: ()] [methods: ==, /=], stock Ord T [context: ()] [methods: compare, <, <=, >, >=, max, min]
    deriving (Eq, Ord)
              │││││││││   └─ $fOrdT ∷ () ⇒ Ord T [superclasses: $fEqT]
              │││││││││   └─ type: T
              │││││││││   └─ compare ∷ T → T → Ordering
              │││││││││   └─ type: T
              │││││││││   └─ type: GHC.Prim.Addr#
              │││││││││   └─ type: GHC.Types.Ordering
              │││││││││   └─ type: GHC.Prim.Addr#
              │││││││││   └─ type: GHC.Types.Ordering
              │││││││││   └─ type: GHC.Prim.Int#
              │││││││││   └─ type: GHC.Prim.Int#
              │││││││││   └─ type: GHC.Prim.Int#
              │││││││││   └─ type: GHC.Prim.Int#
              └─ $fEqT ∷ () ⇒ Eq T
              └─ type: T
              └─ == ∷ T → T → Bool
              └─ type: T
              └─ type: GHC.Prim.Addr#
              └─ type: GHC.Types.Bool
              └─ type: GHC.Prim.Addr#
              └─ type: GHC.Prim.Int#
              └─ type: GHC.Prim.Int#
extensions:
- MagicHash
modules:
- |
  {-# LANGUAGE MagicHash #-}
  module Main where
  import GHC.Classes (Eq, Ord)
  import GHC.Prim (Addr#)
  data T = T Addr#
    deriving (Eq, Ord)
reason: stock Eq and Ord deriving compare an Addr# field with the primitive
  operators eqAddr# and ltAddr#, and the field adds no obligation to the
  inferred context
status: pass
