annotated:
- |-
  module Test where
  import GHC.Classes (Eq (..))
  import GHC.Types (Bool (..))
  instance Eq Bool where
  └─ $fEqBool ∷ () ⇒ Eq Bool
    False == False = True
    ││        └─ type: Bool
    └─ type: Bool
    └─ == ∷ Bool → Bool → Bool
    False == True = False
    ││        └─ type: Bool
    └─ type: Bool
    └─ == ∷ Bool → Bool → Bool
    True == False = False
    ││       └─ type: Bool
    └─ type: Bool
    └─ == ∷ Bool → Bool → Bool
    True == True = True
    ││       └─ type: Bool
    └─ type: Bool
    └─ == ∷ Bool → Bool → Bool
  eqBool :: Bool -> Bool -> Bool
  eqBool = (==)
  │        └─ type: Bool → Bool → Bool; type-args: Bool; evidence: $fEqBool
  └─ type: Bool → Bool → Bool
extensions: []
modules:
- |
  module Test where
  import GHC.Classes (Eq (..))
  import GHC.Types (Bool (..))
  instance Eq Bool where
    False == False = True
    False == True = False
    True == False = False
    True == True = True
  eqBool :: Bool -> Bool -> Bool
  eqBool = (==)
reason: ''
status: pass
