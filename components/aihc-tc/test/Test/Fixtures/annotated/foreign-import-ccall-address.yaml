annotated:
- |-
  module Test where
  import GHC.Prim (Addr#)
  data Ptr a = Ptr Addr#
       │       └─ type: ∀ a. GHC.Prim.Addr# → Ptr a
       └─ type: Type → Type
  data Word8
       └─ type: Type
  foreign import ccall "&hs_table" table :: Ptr Word8
  └─ type: Ptr Word8
  test = table
  └─ type: Ptr Word8
extensions:
- ForeignFunctionInterface
- MagicHash
modules:
- |
  module Test where
  import GHC.Prim (Addr#)
  data Ptr a = Ptr Addr#
  data Word8
  foreign import ccall "&hs_table" table :: Ptr Word8
  test = table
reason: annotated output includes an address ccall import and a later use
status: pass
