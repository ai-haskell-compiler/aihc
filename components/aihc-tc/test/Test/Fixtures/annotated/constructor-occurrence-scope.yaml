annotated:
- |-
  module Test where
  data Truth = True
       │       └─ type: Truth
       └─ type: *
  id x = x
  │  └─ x ∷ a
  └─ type: ∀ a. a → a
  truth = id True
  │       └─ type: Truth → Truth; type-args: Truth
  └─ type: Truth
extensions: []
modules:
- |
  module Test where
  data Truth = True
  id x = x
  truth = id True
reason: constructor occurrence annotations must come from the current scope, not builtin
  spellings
status: pass
