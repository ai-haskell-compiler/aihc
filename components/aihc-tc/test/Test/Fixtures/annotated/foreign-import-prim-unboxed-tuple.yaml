annotated:
- |-
  module Test where
  import GHC.Prim (Int#)
  foreign import prim "tup" tuple :: () -> (# Int#, Int# #)
  └─ type: () → GHC.Types.Tuple2# GHC.Prim.Int# GHC.Prim.Int#
  test = tuple
  └─ type: () → GHC.Types.Tuple2# GHC.Prim.Int# GHC.Prim.Int#
extensions:
- GHCForeignImportPrim
- MagicHash
- UnboxedTuples
modules:
- |
  module Test where
  import GHC.Prim (Int#)
  foreign import prim "tup" tuple :: () -> (# Int#, Int# #)
  test = tuple
reason: annotated output includes a prim import whose result is an unboxed tuple
status: pass
