annotated:
- |-
  module Test where
  f x = let y = x in y
  │ │       └─ y ∷ a
  │ └─ x ∷ a
  └─ f ∷ ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let y = x in y
reason: ''
status: pass
