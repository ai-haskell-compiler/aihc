annotated:
- |-
  module Test where
  f x = let y = x in y
  │ └─ x ∷ a│   │    └─ type: a
  │         │   └─ type: a
  │         └─ type: a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let y = x in y
reason: ''
status: pass
