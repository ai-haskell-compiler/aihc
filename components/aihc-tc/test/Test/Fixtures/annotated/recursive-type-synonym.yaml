annotated:
- |-
  module Test where
  type Loop a = Loop a
  ││    └─ type: Type → Type
  └─ error: recursive type synonym: Loop
  └─ error: recursive type synonym: Loop
extensions: []
modules:
- |
  module Test where
  type Loop a = Loop a
reason: Recursive type synonym detection must terminate.
status: pass
