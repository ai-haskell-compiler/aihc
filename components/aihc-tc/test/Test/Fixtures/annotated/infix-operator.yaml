annotated:
- |-
  module Test where
  infixr 9 .
  (.) f g x = f (g x)
  │   │ │ └─ type: c
  │   │ └─ type: c → a
  │   └─ type: a → b
  └─ type: ∀ a b c. (a → b) → (c → a) → c → b
  f x = x
  │ └─ type: a
  └─ type: ∀ a. a → a
  g x = x
  │ └─ type: a
  └─ type: ∀ a. a → a
  h = f . g
  │   │ │ └─ type: a → a; type-args: a
  │   │ └─ type: (a → a) → (a → a) → a → a; type-args: a, a, a
  │   └─ type: a → a; type-args: a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  infixr 9 .
  (.) f g x = f (g x)
  f x = x
  g x = x
  h = f . g
reason: infix operator definitions typecheck and elaborate their type arguments
status: pass
