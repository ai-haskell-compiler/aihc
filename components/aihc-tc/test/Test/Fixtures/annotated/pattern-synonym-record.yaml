annotated:
- |-
  {-# LANGUAGE PatternSynonyms #-}
  module Test where
  data Pair a = Pair a a
       │        └─ type: ∀ a. a → a → Pair a
       └─ type: Type → Type
  pattern Point {px, py} = Pair px py
  │                        │    │  └─ type: a
  │                        │    └─ type: a
  │                        └─ type: Pair a
  └─ type: ∀ a. a → a → Pair a
  first :: Pair a -> a
  first p = px p
  │     │   └─ type: Pair a → a; type-args: a
  │     └─ type: Pair a
  └─ type: ∀ a. Pair a → a
  swap :: Pair a -> Pair a
  swap p = Point (py p) (px p)
  │    │   │      │      └─ type: Pair a → a; type-args: a
  │    │   │      └─ type: Pair a → a; type-args: a
  │    │   └─ type: a → a → Pair a; type-args: a
  │    └─ type: Pair a
  └─ type: ∀ a. Pair a → Pair a
extensions:
- PatternSynonyms
modules:
- |
  {-# LANGUAGE PatternSynonyms #-}
  module Test where
  data Pair a = Pair a a
  pattern Point {px, py} = Pair px py
  first :: Pair a -> a
  first p = px p
  swap :: Pair a -> Pair a
  swap p = Point (py p) (px p)
reason: a record pattern synonym gives each field a selector with the type of the synonym scrutinee
status: pass
