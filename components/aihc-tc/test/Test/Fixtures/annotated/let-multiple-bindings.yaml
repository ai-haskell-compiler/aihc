annotated:
- |-
  module Test where
  f x = let y = x; z = y in z
  │ └─ x ∷ a│      └─ type: a
  │         └─ type: a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let y = x; z = y in z
reason: ''
status: pass
