extensions: []
modules:
  - |
    module Test where
    fn a@b@c = (a,b,c)
annotated:
  - |-
    module Test where
    fn a@b@c = (a,b,c)
    │  │ │ │   └─ type-args: a, a, a
    │  │ │ └─ c ∷ a
    │  │ └─ b ∷ a
    │  └─ a ∷ a
    └─ fn ∷ ∀ a. a → (a, a, a)
status: pass
reason: ""
