annotated:
- |-
  module Test where
  fn a@b@c = (a,b,c)
  │           ││ │ └─ type: a
  │           ││ └─ type: a
  │           └─ type: (a, a, a); type-args: a, a, a
  │           └─ type: a
  └─ type: ∀ a. a → (a, a, a)
extensions: []
modules:
- |
  module Test where
  fn a@b@c = (a,b,c)
reason: ''
status: pass
