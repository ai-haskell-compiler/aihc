annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ False ∷ Bool
       │      └─ True ∷ Bool
       └─ Bool ∷ *
  f x = (x, x)
  │ │ └─ type-args: a, a
  │ └─ x ∷ a
  └─ f ∷ ∀ a. a → (a, a)
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x = (x, x)
reason: tuple construction infers and renders boxed tuple result types
status: pass
