annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: *
  f x = (x, x)
  │ │    └─ type: (a, a); type-args: a, a
  │ └─ type: a
  └─ type: ∀ a. a → (a, a)
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x = (x, x)
reason: tuple construction infers and renders boxed tuple result types
status: pass
