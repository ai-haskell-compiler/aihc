annotated:
- |-
  module A where
  data Ty = Con
       │    └─ type: Ty
       └─ type: Type
- |-
  module B where
  data Ty = Con
       │    └─ type: Ty
       └─ type: Type
- |-
  module C where
  import qualified A
  import qualified B
  val1 = A.Con
  └─ type: A.Ty
  val2 = B.Con
  └─ type: B.Ty
extensions: []
modules:
- |
  module A where
  data Ty = Con
- |
  module B where
  data Ty = Con
- |
  module C where
  import qualified A
  import qualified B
  val1 = A.Con
  val2 = B.Con
reason: Duplicate unqualified data type interface keys raise an exception when C imports A and B.
status: xfail
