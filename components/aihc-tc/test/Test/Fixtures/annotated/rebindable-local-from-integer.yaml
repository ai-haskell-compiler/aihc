extensions:
- RebindableSyntax
modules:
- |
  {-# LANGUAGE RebindableSyntax #-}
  module Test where
  import GHC.Types (Bool (..))
  value :: Bool
  value =
    let fromInteger _ = True
     in 1
annotated:
- |-
  {-# LANGUAGE RebindableSyntax #-}
  module Test where
  import GHC.Types (Bool (..))
  value :: Bool
  value =
  └─ type: GHC.Types.Bool
    let fromInteger _ = True
        │           └─ type: a
        └─ type: ∀ a. a → GHC.Types.Bool
     in 1
        └─ type: GHC.Types.Bool; type-args: ()
reason: RebindableSyntax takes fromInteger from a local binding in scope
status: pass
