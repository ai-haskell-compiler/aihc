annotated:
- |-
  module Test where
  f x = let g y = y in g x
  │ │ │     │ └─ y ∷ a
  │ │ │     └─ g ∷ ∀ a. a → a
  │ │ └─ type-args: a
  │ └─ x ∷ a
  └─ f ∷ ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g y = y in g x
reason: ''
status: pass
