annotated:
- |-
  module Test where
  f x = let g y = y in g x
  │ └─ x ∷ a│ └─ y ∷ a └─ type-args: a
  │         └─ g ∷ ∀ a. a → a
  └─ f ∷ ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g y = y in g x
reason: ''
status: pass
