annotated:
- |-
  module Test where
  f x = let g y = y in g x
  │         │     │    │ └─ type: a
  │         │     │    └─ type: a → a; type-args: a
  │         │     └─ type: ?5
  │         └─ type: ∀ a. a → a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g y = y in g x
reason: ''
status: pass
