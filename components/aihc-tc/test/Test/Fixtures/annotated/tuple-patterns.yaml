annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: *
  fst (a, b) = a
  │    │  └─ type: b
  │    └─ type: a
  └─ type: ∀ a b. (a, b) → a
  snd (a, b) = b
  │    │  └─ type: b
  │    └─ type: a
  └─ type: ∀ a b. (a, b) → b
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  fst (a, b) = a
  snd (a, b) = b
reason: ''
status: pass
