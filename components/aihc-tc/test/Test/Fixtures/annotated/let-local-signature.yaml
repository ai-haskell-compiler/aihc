annotated:
- |-
  module Test where
  f x = let g :: a -> a; g y = y in g x
  │ │ └─ type-args: a    │ └─ y ∷ a
  │ └─ x ∷ a             └─ g ∷ ∀ a. a → a
  └─ f ∷ ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g :: a -> a; g y = y in g x
reason: ''
status: pass
