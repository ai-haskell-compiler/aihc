annotated:
- |-
  module Test where
  f x = let g :: a -> a; g y = y in g x
  │ └─ x ∷ a             │ └─ y ∷ a └─ type-args: a
  └─ f ∷ ∀ a. a → a      └─ g ∷ ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g :: a -> a; g y = y in g x
reason: ''
status: pass
