annotated:
- |-
  module Test where
  f x = let g :: a -> a; g y = y in g x
  │ └─ type: a           │ │        └─ type: a → a; type-args: a
  └─ type: ∀ a. a → a    │ └─ type: a
                         └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let g :: a -> a; g y = y in g x
reason: ''
status: pass
