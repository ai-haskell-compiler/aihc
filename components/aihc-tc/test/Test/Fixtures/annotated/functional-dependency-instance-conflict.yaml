annotated:
- |-
  module Test where
  data Bag = Bag
       │     └─ type: Bag
       └─ type: Type
  data Nat = Z
       │     └─ type: Nat
       └─ type: Type
  data Flag = Flag
       │      └─ type: Flag
       └─ type: Type
  class Collection c e | c -> e where
  └─ class methods: insert ∷ ∀ c e. (Collection c e) ⇒ e → c → c; fundeps: c → e
    insert :: e -> c -> c
  instance Collection Bag Nat where
  └─ $fCollectionBagNat ∷ () ⇒ Collection Bag Nat
    insert _ bag = bag
    │      │ └─ type: Bag
    │      └─ type: Nat
    └─ insert ∷ Nat → Bag → Bag
  instance Collection Bag Flag where
  │        └─ error: instance Collection Bag Flag conflicts with instance Collection Bag Nat under the functional dependency c → e
  └─ $fCollectionBagFlag ∷ () ⇒ Collection Bag Flag
    insert _ bag = bag
    │      │ └─ type: Bag
    │      └─ type: Flag
    └─ insert ∷ Flag → Bag → Bag
extensions:
- MultiParamTypeClasses
- FunctionalDependencies
modules:
- |
  module Test where
  data Bag = Bag
  data Nat = Z
  data Flag = Flag
  class Collection c e | c -> e where
    insert :: e -> c -> c
  instance Collection Bag Nat where
    insert _ bag = bag
  instance Collection Bag Flag where
    insert _ bag = bag
reason: two instances that agree on the determining parameter must agree on the parameter
  that the dependency determines
status: pass
