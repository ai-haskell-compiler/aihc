annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ False ∷ Bool
       │      └─ True ∷ Bool
       └─ Bool ∷ *
  f x y = x + y
  │ │ │     └─ type-args: a, b
  │ │ └─ y ∷ b
  │ └─ x ∷ a
  └─ f ∷ ∀ a b. a → b → a
    where
      (+) a b = a
       │  │ └─ b ∷ b
       │  └─ a ∷ a
       └─ (+) ∷ ∀ a b. a → b → a
      zero = True
      └─ zero ∷ Bool
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x y = x + y
    where
      (+) a b = a
      zero = True
reason: ''
status: pass
