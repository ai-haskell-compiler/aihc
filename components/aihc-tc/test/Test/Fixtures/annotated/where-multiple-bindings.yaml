annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: *
  f x y = x + y
  │       │ │ └─ type: b
  │       │ └─ type: a → b → a; type-args: a, b
  │       └─ type: a
  └─ type: ∀ a b. a → b → a
    where
      (+) a b = a
       │        └─ type: ?8
       └─ type: ∀ a b. a → b → a
      zero = True
      │      └─ type: Bool
      └─ type: Bool
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x y = x + y
    where
      (+) a b = a
      zero = True
reason: ''
status: pass
