annotated:
- |-
  module Test where
  data Bool = True | False
  └─ type: *  │      └─ type: Bool
              └─ type: Bool
  f x y = x + y
  │ │ │     └─ type: a → b → a; type-args: a, b
  │ │ └─ type: b
  │ └─ type: a
  └─ type: ∀ a b. a → b → a
    where
      (+) a b = a
      │   │ └─ type: ?7
      │   └─ type: ?8
      └─ type: ∀ a b. a → b → a
      zero = True
      └─ type: Bool
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x y = x + y
    where
      (+) a b = a
      zero = True
reason: ''
status: pass
