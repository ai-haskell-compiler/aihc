annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ False ∷ Bool
       │      └─ True ∷ Bool
       └─ Bool ∷ *
  f x = x + one
  │ └─ x ∷ a
  └─ f ∷ ∀ a. a → a
    where
      (+) a b = a
      │   │ └─ b ∷ b
      │   └─ a ∷ a
      └─ (+) ∷ ∀ a b. a → b → a
      one = True
      └─ one ∷ Bool
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x = x + one
    where
      (+) a b = a
      one = True
reason: ''
status: pass
