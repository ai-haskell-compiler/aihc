annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: *
  f x = x + one
  │ │     └─ type: a → Bool → a; type-args: a, Bool
  │ └─ x ∷ a
  └─ type: ∀ a. a → a
    where
      (+) a b = a
       │  │ └─ b ∷ ?6
       │  └─ a ∷ ?7
       └─ type: ∀ a b. a → b → a
      one = True
      └─ type: Bool
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x = x + one
    where
      (+) a b = a
      one = True
reason: ''
status: pass
