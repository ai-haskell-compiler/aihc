annotated:
- |-
  module Test where
  data Bool = True | False
  └─ type: *  │      └─ type: Bool
              └─ type: Bool
  id x = x
  │  └─ type: a
  └─ type: ∀ a. a → a
  const x y = x
  │     │ └─ type: b
  │     └─ type: a
  └─ type: ∀ a b. a → b → a
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  id x = x
  const x y = x
reason: ''
status: pass
