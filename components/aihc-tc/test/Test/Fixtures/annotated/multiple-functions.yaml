annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: *
  id x = x
  │  └─ x ∷ a
  └─ type: ∀ a. a → a
  const x y = x
  │     │ └─ y ∷ b
  │     └─ x ∷ a
  └─ type: ∀ a b. a → b → a
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  id x = x
  const x y = x
reason: ''
status: pass
