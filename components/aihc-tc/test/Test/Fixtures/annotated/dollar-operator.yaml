annotated:
- |-
  module Test where
  f $ x = f x
  │ │ └─ x ∷ a
  │ └─ ($) ∷ ∀ a b. (a → b) → a → b
  └─ f ∷ a → b
extensions: []
modules:
- |
  module Test where
  f $ x = f x
reason: ''
status: pass
