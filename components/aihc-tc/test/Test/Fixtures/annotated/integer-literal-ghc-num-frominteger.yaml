annotated:
- |-
  module GHC.Num where
  import GHC.Prim (Int#)
  data Integer = IS Int#
       │         └─ type: Int# → Integer
       └─ type: Type
  class Num a where
  └─ class methods: fromInteger ∷ ∀ a. (Num a) ⇒ Integer → a
    fromInteger :: Integer -> a
- |-
  module Main where
  import GHC.Num (fromInteger)
  one = 1
  │     └─ type: a; type-args: a; evidence: given Num a
  └─ type: ∀ a. (GHC.Num.Num a) ⇒ a
extensions:
- MagicHash
modules:
- |
  module GHC.Num where
  import GHC.Prim (Int#)
  data Integer = IS Int#
  class Num a where
    fromInteger :: Integer -> a
- |
  module Main where
  import GHC.Num (fromInteger)
  one = 1
reason: integer literals use GHC.Num.fromInteger and infer a Num-constrained result
  type
status: pass
