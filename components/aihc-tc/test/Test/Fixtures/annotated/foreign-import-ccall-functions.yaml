annotated:
- |-
  module Test where
  data CInt
  └─ type: *
  data IO a
  └─ type: * → *
  foreign import ccall safe "plus1" plus1 :: CInt -> IO CInt
  └─ type: CInt → IO CInt
  foreign import capi interruptible "math.h plus2" plus2 :: CInt -> CInt -> IO CInt
  └─ type: CInt → CInt → IO CInt
  test1 = plus1
  └─ type: CInt → IO CInt
  test2 = plus2
  └─ type: CInt → CInt → IO CInt
extensions:
- ForeignFunctionInterface
modules:
- |
  module Test where
  data CInt
  data IO a
  foreign import ccall safe "plus1" plus1 :: CInt -> IO CInt
  foreign import capi interruptible "math.h plus2" plus2 :: CInt -> CInt -> IO CInt
  test1 = plus1
  test2 = plus2
reason: annotated output includes ccall and capi imported functions and later uses
status: pass
