annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: Type
  f x | True = x
  │ └─ type: a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  f x | True = x
reason: a boolean guard leaves the guarded equation's argument fully polymorphic
status: pass
