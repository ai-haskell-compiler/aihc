annotated:
- |-
  {-# LANGUAGE PatternSynonyms #-}
  module Test where
  data L a = N | C a (L a)
       │     │   └─ type: ∀ a. a → L a → L a
       │     └─ type: ∀ a. L a
       └─ type: Type → Type
  pattern Head :: a -> L a
  pattern Head x <- C x _
  │                 │ │ └─ type: L a
  │                 │ └─ type: a
  │                 └─ type: L a
  └─ type: ∀ a. a → L a
  first :: L a -> a
  first (Head x) = x
  │      │    └─ type: a
  │      └─ type: L a
  └─ type: ∀ a. L a → a

extensions:
- PatternSynonyms
modules:
- |
  {-# LANGUAGE PatternSynonyms #-}
  module Test where
  data L a = N | C a (L a)
  pattern Head :: a -> L a
  pattern Head x <- C x _
  first :: L a -> a
  first (Head x) = x
reason: a pattern synonym signature gives the matcher type and the builder type
status: pass
