annotated:
- |-
  module Test where
  f x = let loop y = loop y in x
  │ │       │    └─ y :: a
  │ │       └─ loop :: forall a b. a -> b
  │ └─ x :: a
  └─ f :: forall a. a -> a
extensions: []
modules:
- |
  module Test where
  f x = let loop y = loop y in x
reason: ''
status: pass
