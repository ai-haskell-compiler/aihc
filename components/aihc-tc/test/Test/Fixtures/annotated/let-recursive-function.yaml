annotated:
- |-
  module Test where
  f x = let loop y = loop y in x
  │ └─ x ∷ a│    └─ y ∷ ?4
  │         └─ type: ∀ a b. a → b
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let loop y = loop y in x
reason: ''
status: pass
