annotated:
- |-
  module Test where
  f x = let loop y = loop y in x
  │ │       │    │   └─ type: a → b; type-args: a, b
  │ │       │    └─ type: a
  │ │       └─ type: ∀ a b. a → b
  │ └─ type: a
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let loop y = loop y in x
reason: ''
status: pass
