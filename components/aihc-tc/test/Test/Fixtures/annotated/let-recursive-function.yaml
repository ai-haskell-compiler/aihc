annotated:
- |-
  module Test where
  f x = let loop y = loop y in x
  │         │        │    │    └─ type: a
  │         │        │    └─ type: ?4
  │         │        └─ type: ?4 → ?5
  │         └─ type: ∀ a b. a → b
  └─ type: ∀ a. a → a
extensions: []
modules:
- |
  module Test where
  f x = let loop y = loop y in x
reason: ''
status: pass
