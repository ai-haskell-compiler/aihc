annotated:
- |-
  module Test where
  foreign import prim (+#) :: Int# -> Int# -> Int#
                       └─ type: Int# → Int# → Int#
  int# = 10#
  └─ type: Int#
  word# = 10##
  └─ type: Word#
extensions:
- MagicHash
modules:
- |
  module Test where
  foreign import prim (+#) :: Int# -> Int# -> Int#
  int# = 10#
  word# = 10##
reason: foreign import prim registers unboxed primitive functions and MagicHash literals
  keep their unboxed types
status: pass
