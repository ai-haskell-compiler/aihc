annotated:
- |-
  module Test where
  import GHC.Prim (Int#)
  foreign import prim (+#) :: Int# -> Int# -> Int#
  └─ type: GHC.Prim.Int# → GHC.Prim.Int# → GHC.Prim.Int#
  int# = 10#
  ││      └─ type: GHC.Prim.Int#
  └─ type: GHC.Prim.Int#
  └─ error: top-level binding int# has unlifted type Int#
  word# = 10##
  ││       └─ type: GHC.Prim.Word#
  └─ type: GHC.Prim.Word#
  └─ error: top-level binding word# has unlifted type Word#
extensions:
- MagicHash
modules:
- |
  module Test where
  import GHC.Prim (Int#)
  foreign import prim (+#) :: Int# -> Int# -> Int#
  int# = 10#
  word# = 10##
reason: foreign primitives may have unlifted types but source top-level unboxed values
  are rejected
status: pass
