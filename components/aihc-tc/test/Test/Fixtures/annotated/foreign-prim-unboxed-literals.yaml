annotated:
- |-
  module Test where
  foreign import prim (+#) :: Int# -> Int# -> Int#
  └─ type: Int# → Int# → Int#
  int# = 10#
  └─ error: top-level binding int# has unlifted type Int#
  └─ type: Int#
  word# = 10##
  └─ error: top-level binding word# has unlifted type Word#
  └─ type: Word#
extensions:
- MagicHash
modules:
- |
  module Test where
  foreign import prim (+#) :: Int# -> Int# -> Int#
  int# = 10#
  word# = 10##
reason: foreign primitives may have unlifted types but source top-level unboxed values are rejected
status: pass
