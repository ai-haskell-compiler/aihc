annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: Type
  val = case () of
    () -> True
    () -> ()
          └─ error: expression has type (), but expected Bool from a case branch
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  val = case () of
    () -> True
    () -> ()
reason: case branch result mismatches should explain where the expected type came
  from
status: pass
