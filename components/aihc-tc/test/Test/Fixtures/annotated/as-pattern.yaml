annotated:
- |-
  module Test where
  asPattern xs@(x:rest) = xs
  │         │   │ └─ type: [a]
  │         │   └─ type: a
  │         └─ type: [a]
  └─ type: ∀ a. [a] → [a]
extensions: []
modules:
- |
  module Test where
  asPattern xs@(x:rest) = xs
reason: ''
status: pass
