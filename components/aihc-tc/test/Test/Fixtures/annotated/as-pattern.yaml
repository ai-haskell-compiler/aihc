annotated:
- |-
  module Test where
  asPattern xs@(x:rest) = xs
  └─ type: ∀ a. [a] → [a] └─ type: [a]
extensions: []
modules:
- |
  module Test where
  asPattern xs@(x:rest) = xs
reason: ''
status: pass
