extensions: []
modules:
  - |
    module Test where
    asPattern xs@(x:rest) = xs
annotated:
  - |-
    module Test where
    asPattern xs@(x:rest) = xs
    │         │   │ └─ rest ∷ [a]
    │         │   └─ x ∷ a
    │         └─ xs ∷ [a]
    └─ asPattern ∷ ∀ a. [a] → [a]
status: pass
reason: ""
