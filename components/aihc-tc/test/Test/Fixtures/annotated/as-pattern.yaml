annotated:
- |-
  module Test where
  asPattern xs@(x:rest) = xs
  │         │   │ └─ rest ∷ [a]
  │         │   └─ x ∷ a
  │         └─ xs ∷ [a]
  └─ type: ∀ a. [a] → [a]
extensions: []
modules:
- |
  module Test where
  asPattern xs@(x:rest) = xs
reason: ''
status: pass
