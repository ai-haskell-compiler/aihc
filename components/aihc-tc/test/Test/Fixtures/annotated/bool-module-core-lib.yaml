annotated:
- |-
  module Test where
  data Bool = False | True
       │      │       └─ type: Bool
       │      └─ type: Bool
       └─ type: Type
  infixr 3 &&
  (&&) :: Bool -> Bool -> Bool
  False && _ = False
  ││        └─ type: Bool
  └─ type: Bool → Bool → Bool
  └─ type: Bool
  True && x = x
  ││       └─ type: Bool
  └─ type: Bool → Bool → Bool
  └─ type: Bool
  not :: Bool -> Bool
  not False = True
  │   └─ type: Bool
  └─ type: Bool → Bool
  not True = False
  │   └─ type: Bool
  └─ type: Bool → Bool
  otherwise :: Bool
  otherwise = True
  └─ type: Bool
  infixr 2 ||
  (||) :: Bool -> Bool -> Bool
  False || x = x
  ││        └─ type: Bool
  └─ type: Bool → Bool → Bool
  └─ type: Bool
  True || _ = True
  ││       └─ type: Bool
  └─ type: Bool → Bool → Bool
  └─ type: Bool
extensions: []
modules:
- |
  module Test where
  data Bool = False | True
  infixr 3 &&
  (&&) :: Bool -> Bool -> Bool
  False && _ = False
  True && x = x
  not :: Bool -> Bool
  not False = True
  not True = False
  otherwise :: Bool
  otherwise = True
  infixr 2 ||
  (||) :: Bool -> Bool -> Bool
  False || x = x
  True || _ = True
reason: types the Bool API exported by Data.Bool
status: pass
