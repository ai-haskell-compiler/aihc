annotated:
- |-
  module Test where
  (++) :: [a] -> [a] -> [a]
  [] ++ bs = bs
     │  └─ bs ∷ [a]
     └─ type: ∀ a. [a] → [a] → [a]
  (a:as) ++ bs = a : (as ++ bs)
   │ │   │  │      │     └─ type: [a] → [a] → [a]; type-args: a
   │ │   │  │      └─ type: a → [a] → [a]; type-args: a
   │ │   │  └─ bs ∷ [a]
   │ │   └─ type: ∀ a. [a] → [a] → [a]
   │ └─ as ∷ [a]
   └─ a ∷ a
extensions: []
modules:
- |
  module Test where
  (++) :: [a] -> [a] -> [a]
  [] ++ bs = bs
  (a:as) ++ bs = a : (as ++ bs)
reason: polymorphic list signatures quantify variables inside list types
status: pass
