annotated:
- |-
  module Test where
  data Word64#
       └─ type: Type
  data Word64 = W64# Word64#
       │        └─ type: Word64# → Word64
       └─ type: Type
  foreign import ccall unsafe "rdtsc.h" rdtsc :: Word64
  └─ type: Word64
  test = rdtsc
  └─ type: Word64
extensions:
- ForeignFunctionInterface
- MagicHash
modules:
- |
  module Test where
  data Word64#
  data Word64 = W64# Word64#
  foreign import ccall unsafe "rdtsc.h" rdtsc :: Word64
  test = rdtsc
reason: annotated output includes a ccall imported value and a later use
status: pass
