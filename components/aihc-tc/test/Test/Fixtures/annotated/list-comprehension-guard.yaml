annotated:
- |-
  module Test where
  import GHC.Types (Bool)
  filter f list = [ x | x <- list, f x ]
  │      │ │      │     └─ type: a
  │      │ │      └─ type: [a]; type-args: a
  │      │ └─ type: [a]
  │      └─ type: a → GHC.Types.Bool
  └─ type: ∀ a. (a → GHC.Types.Bool) → [a] → [a]
extensions: []
modules:
- |
  module Test where
  import GHC.Types (Bool)
  filter f list = [ x | x <- list, f x ]
reason: list comprehension guards must typecheck as Bool
status: pass
