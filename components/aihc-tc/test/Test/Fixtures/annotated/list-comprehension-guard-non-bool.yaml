annotated:
- |-
  module Test where
  bad xs = [ x | x <- xs, () ]
                          └─ error: couldn't match () with Bool
extensions: []
modules:
- |
  module Test where
  bad xs = [ x | x <- xs, () ]
reason: list comprehension guards must be Bool, not Int
status: pass
