extensions: []
modules:
  - |
    module Test where
    bad xs = [ x | x <- xs, 1 ]
annotated:
  - |-
    module Test where
    bad xs = [ x | x <- xs, 1 ]
                            └─ error: couldn't match Int with Bool
status: pass
reason: list comprehension guards must be Bool, not Int
