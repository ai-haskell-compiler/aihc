extensions: []
modules:
  - |
    module Test where
    bad xs = [ x | x <- xs, 1 ]
annotated:
  - |-
    module Test where
    └─ error: couldn't match Int with Bool
    bad xs = [ x | x <- xs, 1 ]
status: pass
reason: list comprehension guards must be Bool, not Int
