extensions: []
modules:
  - |
    module Test where
    bad xs = [ x | x <- xs, () ]
annotated:
  - |-
    module Test where
    bad xs = [ x | x <- xs, () ]
                            └─ error: couldn't match () with Bool
status: pass
reason: list comprehension guards must be Bool, not Int
