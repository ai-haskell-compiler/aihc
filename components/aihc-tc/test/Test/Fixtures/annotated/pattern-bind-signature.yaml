annotated:
- |-
  module Test where
  data Bool = False | True
       │      │       └─ type: Bool
       │      └─ type: Bool
       └─ type: *

  same :: Bool -> Bool -> Bool
  same x _ = x
  │    └─ x ∷ Bool
  └─ type: Bool → Bool → Bool

  eqBool :: Bool -> Bool -> Bool
  eqBool = same
  └─ type: Bool → Bool → Bool
extensions: []
modules:
- |
  module Test where
  data Bool = False | True

  same :: Bool -> Bool -> Bool
  same x _ = x

  eqBool :: Bool -> Bool -> Bool
  eqBool = same
reason: applies a type signature to a bare top-level pattern binding
status: pass
