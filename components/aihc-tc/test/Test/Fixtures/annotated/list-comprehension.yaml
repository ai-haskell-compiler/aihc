annotated:
- |-
  module Test where
  map f list = [ f x | x <- list ]
  │   │ │        │ │   │    └─ type: [a]
  │   │ │        │ │   └─ x ∷ a
  │   │ │        │ └─ type: a
  │   │ │        └─ type: a → b
  │   │ └─ list ∷ [a]
  │   └─ f ∷ a → b
  └─ type: ∀ a b. (a → b) → [a] → [b]
extensions: []
modules:
- |
  module Test where
  map f list = [ f x | x <- list ]
reason: list comprehensions are traversed by type-checker annotation rendering
status: pass
