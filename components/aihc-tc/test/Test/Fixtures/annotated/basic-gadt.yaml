annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ type: Bool
       │      └─ type: Bool
       └─ type: *
  data GADT a where
       └─ type: * → *
    IsBool :: GADT Bool
    └─ type: GADT Bool
  not True = False
  └─ type: Bool → Bool
  not False = True
  └─ type: Bool → Bool

  fn :: a -> GADT a -> a
  fn x IsBool = not x
  │  └─ x ∷ a
  └─ type: ∀ a. a → GADT a → a
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  data GADT a where
    IsBool :: GADT Bool
  not True = False
  not False = True

  fn :: a -> GADT a -> a
  fn x IsBool = not x
status: pass
