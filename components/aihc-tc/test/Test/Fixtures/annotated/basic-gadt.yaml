annotated:
- |-
  module Test where
  data Bool = True | False
       │      │      └─ False ∷ Bool
       │      └─ True ∷ Bool
       └─ Bool ∷ *
  data GADT a where
       └─ GADT ∷ *
    IsBool :: GADT Bool
    └─ IsBool ∷ GADT Bool
  not True = False
  └─ not ∷ Bool → Bool
  not False = True
  └─ not ∷ Bool → Bool
  
  fn :: a -> GADT a -> a
  fn x IsBool = not x
  │  └─ x ∷ a
  └─ fn ∷ ∀ a. a → GADT a → a
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  data GADT a where
    IsBool :: GADT Bool
  not True = False
  not False = True

  fn :: a -> GADT a -> a
  fn x IsBool = not x
status: pass
