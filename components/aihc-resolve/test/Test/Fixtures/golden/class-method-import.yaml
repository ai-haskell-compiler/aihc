---
annotated:
- |-
  module ClassMod where
  class Box a where
        └─ t ClassMod
    pick :: a -> a
- |-
  module Main where
  import ClassMod (Box(pick))
  f = pick
  │   └─ v ClassMod
  └─ v Main
extensions: []
modules:
- |
  module ClassMod where
  class Box a where
    pick :: a -> a
- |
  module Main where
  import ClassMod (Box (pick))

  f = pick
reason: ''
status: pass
