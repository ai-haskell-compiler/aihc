annotated:
- |-
  module Main where
  data Person = Person
       │        └─ v Main
       └─ t Main
    { name :: String
              └─ t Error unbound
    , age :: Int
             └─ t Error unbound
    , email :: String
               └─ t Error unbound
    }
  getAge :: Person -> Int
  └─ v Main └─ t Main └─ t Error unbound
  getAge (Person { name = n, .. }) = age
  │       └─ v Main       └─ v 0     └─ v 1
  └─ v Main
extensions:
- RecordWildCards
modules:
- |
  module Main where
  data Person = Person
    { name :: String
    , age :: Int
    , email :: String
    }
  getAge :: Person -> Int
  getAge (Person { name = n, .. }) = age
status: pass
