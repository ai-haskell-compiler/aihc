annotated:
- |-
  module Main where
  data Person = Person
       │        └─ v Main
       └─ t Main
    { name :: String
              └─ t Error unbound
    , age :: Int
             └─ t Error unbound
    }
  getAge :: Person -> Int
  └─ v Main └─ t Main └─ t Error unbound
  getAge (Person { name = n, age = a }) = a
  └─ v Main               └─ v 0   └─ v 1 └─ v 1
extensions: []
modules:
- |
  module Main where
  data Person = Person
    { name :: String
    , age :: Int
    }
  getAge :: Person -> Int
  getAge (Person { name = n, age = a }) = a
reason: ''
status: pass
