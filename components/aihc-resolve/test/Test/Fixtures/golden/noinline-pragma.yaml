extensions: []
modules:
  - |
    module Main where
    x = 1
    {-# NOINLINE x #-}
annotated:
  - |
    module Main where
    x = 1
    │   └─ v Error unbound
    └─ v Main
    {-# NOINLINE x #-}
status: pass
reason: NOINLINE is binding metadata and must not be rejected as unhandled syntax
