extensions: []
modules:
  - |
    module Main where
    x = 1
    {-# NOINLINE x #-}
annotated:
  - |
    module Main where
    x = 1
    │   └─ v Error unbound
    └─ v Main
    {-# NOINLINE x #-}
status: pass
reason: NOINLINE is accepted without resolver annotations or diagnostics
