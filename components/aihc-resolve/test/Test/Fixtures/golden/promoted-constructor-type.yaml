extensions:
  - DataKinds
modules:
  - |
    module Main where
    data Levity = Lifted
    type LiftedRep = 'Lifted
annotated:
  - |
    module Main where
    data Levity = Lifted
         │        └─ v Main
         └─ t Main
    type LiftedRep = 'Lifted
         └─ t Main    └─ v Main
status: pass
reason: promoted data constructors resolve in type expressions
