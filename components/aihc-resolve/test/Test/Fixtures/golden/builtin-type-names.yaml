annotated:
- |-
  module Main where
  import GHC.Exts (Int#, addInt#)
  └─ m Error not found
  f :: Int# -> Int#
  │    │       └─ t Builtin Int#
  │    └─ t Builtin Int#
  └─ v Main
  f x# = addInt# x# 1#
  │ │    │       └─ v 0
  │ │    └─ v Error unbound
  │ └─ v 0
  └─ v Main
extensions:
- MagicHash
modules:
- |
  module Main where
  import GHC.Exts (Int#, addInt#)
  f :: Int# -> Int#
  f x# = addInt# x# 1#
reason: ''
status: pass
