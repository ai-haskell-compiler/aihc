---
extensions: []
modules:
- |
  module Main where
  fst3 :: (a, b, c) -> a
  fst3 (x, _, _) = x
annotated:
- |
  module Main where
  fst3 :: (a, b, c) -> a
  └─ v Main
  fst3 (x, _, _) = x
  │     └─ v 0     └─ v 0
  └─ v Main
status: pass
reason: ''
