---
annotated:
- |-
  module Data.Config where
  defaultConfig = 42
  └─ v Data.Config
  maxRetries = 3
  └─ v Data.Config
- |-
  module Main where
  import qualified Data.Config as Config
  useConfig = Config.defaultConfig
  └─ v Main   └─ v Data.Config
        + Config.maxRetries
        │ └─ v Data.Config
        └─ v Error unbound
extensions: []
modules:
- |
  module Data.Config where
  defaultConfig = 42
  maxRetries = 3
- |
  module Main where
  import qualified Data.Config as Config
  useConfig = Config.defaultConfig + Config.maxRetries
status: pass
