---
annotated:
- |-
  module Main where
  import ValidModule (missing_fn)
  └─ v Error not exported
  x = missing_fn
  │   └─ v Error unbound
  └─ v Main
- |-
  module ValidModule where
  valid_fn :: Int
  │           └─ t Error unbound
  └─ v ValidModule
  valid_fn = 1
  └─ v ValidModule
extensions: []
modules:
- |
  module ValidModule where
  valid_fn :: Int
  valid_fn = 1
- |
  module Main where
  import ValidModule (missing_fn)
  x = missing_fn
reason: ''
status: pass
