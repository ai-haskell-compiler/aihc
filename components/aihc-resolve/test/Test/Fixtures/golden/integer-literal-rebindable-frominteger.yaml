annotated:
- |-
  module GHC.Prim where
  data Int#
       └─ t GHC.Prim
- |-
  {-# LANGUAGE RebindableSyntax #-}
  module Main where
  import GHC.Prim (Int#)
  data Integer = IS Int#
       └─ t Main │  └─ t GHC.Prim
                 └─ v Main
  class Num a where
        └─ t Main
    fromInteger :: Integer -> a
    └─ v Main      └─ t Main
  one = 1
  │     └─ v Main
  └─ v Main
extensions:
- MagicHash
modules:
- |
  module GHC.Prim where
  data Int#
- |
  {-# LANGUAGE RebindableSyntax #-}
  module Main where
  import GHC.Prim (Int#)
  data Integer = IS Int#
  class Num a where
    fromInteger :: Integer -> a
  one = 1
reason: RebindableSyntax integer literals resolve the ordinary in-scope fromInteger
status: pass
