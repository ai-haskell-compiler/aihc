extensions:
  - LambdaCase
  - MagicHash
modules:
  - |
    module GHC.Prim where

    data Int#
  - |
    module GHC.Classes where

    data Bool = False | True

    class Eq a where
      (==) :: a -> a -> Bool
  - |
    module GHC.Num where

    import GHC.Prim (Int#)

    data Integer = IS Int#

    class Num a where
      fromInteger :: Integer -> a
      negate :: a -> a
  - |
    module Main where

    fromInteger value = value
    negate value = value
    left == right = ()

    one = 1

    match = \case
      -1 -> ()
annotated:
- |-
  module GHC.Classes where

  data Bool = False | True
       │      │       └─ v GHC.Classes
       │      └─ v GHC.Classes
       └─ t GHC.Classes

  class Eq a where
        └─ t GHC.Classes
    (==) :: a -> a -> Bool
     └─ v GHC.Classes └─ t GHC.Classes
- |-
  module GHC.Num where

  import GHC.Prim (Int#)

  data Integer = IS Int#
       │         │  └─ t GHC.Prim
       │         └─ v GHC.Num
       └─ t GHC.Num

  class Num a where
        └─ t GHC.Num
    fromInteger :: Integer -> a
    └─ v GHC.Num   └─ t GHC.Num
    negate :: a -> a
    └─ v GHC.Num
- |-
  module GHC.Prim where

  data Int#
       └─ t GHC.Prim
- |-
  module Main where

  fromInteger value = value
  └─ v Main   └─ v 0  └─ v 0
  negate value = value
  │      └─ v 1  └─ v 1
  └─ v Main
  left == right = ()
  │    │  └─ v 3  └─ v Builtin ()
  │    └─ v Main
  └─ v 2

  one = 1
  │     └─ t GHC.Num
  │     └─ v GHC.Num
  └─ v Main

  match = \case
  └─ v Main
    -1 -> ()
    ││││     └─ v Builtin ()
    └─ v GHC.Num
    └─ t GHC.Num
    └─ v GHC.Num
    └─ v GHC.Classes
status: pass
reason: ordinary integer syntax resolves canonical methods from GHC.Num and
  GHC.Classes without importing or defining Prelude
