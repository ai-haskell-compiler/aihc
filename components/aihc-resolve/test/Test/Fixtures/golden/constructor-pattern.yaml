---
annotated:
- |-
  module Main where
  data Tree a = Leaf a | Node (Tree a) (Tree a)
       │        └─ v Main│     │        └─ t Main
       └─ t Main         │     └─ t Main
                         └─ v Main
  sumTree :: Tree Int -> Int
  └─ v Main  │    │      └─ t Error unbound
             │    └─ t Error unbound
             └─ t Main
  sumTree (Leaf x) = x
  └─ v Main     │    └─ v 0
                └─ v 0
  sumTree (Node l r) = sumTree
  └─ v Main     │ │    └─ v Main
                │ └─ v 2
                └─ v 1
          l
          └─ v 1
        + sumTree
        │ └─ v Main
        └─ v Error unbound
          r
          └─ v 2
extensions: []
modules:
- |
  module Main where
  data Tree a = Leaf a | Node (Tree a) (Tree a)
  sumTree :: Tree Int -> Int
  sumTree (Leaf x) = x
  sumTree (Node l r) = sumTree l + sumTree r
reason: ''
status: pass
