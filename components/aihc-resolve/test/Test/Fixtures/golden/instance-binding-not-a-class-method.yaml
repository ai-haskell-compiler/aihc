extensions: []
modules:
- |
  module Cls where
  class Shape a where
    area :: a -> a
  other :: a -> a
  other value = value
- |
  module Main where
  import Cls (Shape, other)
  data Box = Box
  instance Shape Box where
    area value = value
    other = Box
annotated:
- |-
  module Cls where
  class Shape a where
        └─ t Cls
    area :: a -> a
    └─ v Cls
  other :: a -> a
  └─ v Cls
  other value = value
  │     └─ v 0  └─ v 0
  └─ v Cls
- |-
  module Main where
  import Cls (Shape, other)
  data Box = Box
       │     └─ v Main
       └─ t Main
  instance Shape Box where
           │     └─ t Main
           └─ t Cls
    area value = value
    │    └─ v 0  └─ v 0
    └─ v Cls
    other = Box
    │       └─ v Main
    └─ v Error not a method of the class Shape
status: pass
reason: An instance binding that is not a method of the class is an error, even when a term with that name is in scope
