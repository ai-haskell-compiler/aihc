annotated:
- |-
  module Main where
  data List a = Nil | (:) a (List a)
       └─ t Main│      │     └─ t Main
                │      └─ v Main
                └─ v Main
  prepend :: a -> List a -> List a
  └─ v Main       └─ t Main └─ t Main
  prepend x xs = (:) x xs
  │       │ └─ v 1│  │ └─ v 1
  │       └─ v 0  │  └─ v 0
  └─ v Main       └─ v Main
extensions: []
modules:
- |
  module Main where
  data List a = Nil | (:) a (List a)
  prepend :: a -> List a -> List a
  prepend x xs = (:) x xs
reason: ''
status: pass
