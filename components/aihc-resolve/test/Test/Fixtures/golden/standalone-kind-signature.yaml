annotated:
- |-
  module Main where
  type Identity :: Type -> Type
       └─ t Main   │       └─ t Builtin Type
                   └─ t Builtin Type
  type Identity a = a
       └─ t Main
  x :: Identity Int
  │    │        └─ t Error unbound
  │    └─ t Main
  └─ v Main
  x = 42
  │   └─ v Error unbound
  └─ v Main
extensions:
- StandaloneKindSignatures
modules:
- |
  module Main where
  type Identity :: Type -> Type
  type Identity a = a
  x :: Identity Int
  x = 42
reason: ''
status: pass
