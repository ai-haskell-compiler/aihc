---
annotated:
- |-
  module Main where
  type Identity :: Type -> Type
       └─ v Error unhandled syntax
  type Identity a = a
       └─ t Main
  x :: Identity Int
  │    │        └─ t Error unbound
  │    └─ t Main
  └─ v Main
  x = 42
  └─ v Main
extensions:
- StandaloneKindSignatures
modules:
- |
  module Main where
  type Identity :: Type -> Type
  type Identity a = a
  x :: Identity Int
  x = 42
reason: ''
status: pass
