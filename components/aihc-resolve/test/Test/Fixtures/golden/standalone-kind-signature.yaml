annotated:
- |-
  module GHC.Types where
  data Type
       └─ t GHC.Types
- |-
  module Main where
  import GHC.Types (Type)
  type Identity :: Type -> Type
       └─ t Main   │       └─ t GHC.Types
                   └─ t GHC.Types
  type Identity a = a
       └─ t Main
  x :: Identity Int
  │    │        └─ t Error unbound
  │    └─ t Main
  └─ v Main
  x = 42
  │   └─ v Error unbound
  └─ v Main
extensions:
- StandaloneKindSignatures
modules:
- |
  module GHC.Types where
  data Type
- |
  module Main where
  import GHC.Types (Type)
  type Identity :: Type -> Type
  type Identity a = a
  x :: Identity Int
  x = 42
reason: ''
status: pass
