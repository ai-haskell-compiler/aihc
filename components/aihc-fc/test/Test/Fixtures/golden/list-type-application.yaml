extensions: []
modules:
  - |
    module Test where
    data Bool = True | False

    xs :: [Bool]
    xs = [True]
expected: |-
  data Bool
   = True
   | False

  xs : [Bool] =
    (((: : ∀ (a : TYPE BoxedRep Lifted). a → [a] → [a]) @Bool) (Test.True : Bool)) (([] : ∀ (a : TYPE BoxedRep Lifted). [a]) @Bool)
status: pass
reason: reifies list constructor type arguments from type-checker annotations
