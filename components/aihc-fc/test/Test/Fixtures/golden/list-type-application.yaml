extensions: []
modules:
  - |
    module Test where
    data Bool = True | False

    xs :: [Bool]
    xs = [True]
expected: |-
  core 2
  prefix 1 = Test

  data Bool where
    | True
    | False;

  xs : tycon "[]" Bool =
    ((":" : ∀ a. a → tycon "[]" a → tycon "[]" a) @Bool) (1.True : Bool) (("[]" : ∀ a. tycon "[]" a) @Bool);
status: pass
reason: reifies list constructor type arguments from type-checker annotations
