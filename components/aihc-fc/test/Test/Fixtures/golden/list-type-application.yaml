extensions: []
modules:
  - |
    module Test where
    data Bool = True | False

    xs :: [Bool]
    xs = [True]
expected: |-
  ":" : ∀ a. a → tycon "[]" a → tycon "[]" a;
  "[]" : ∀ a. tycon "[]" a;

  data Bool where
    | True :: Bool
    | False :: Bool;

  xs : tycon "[]" Bool =
    (var ":" @Bool) True (var "[]" @Bool);
status: pass
reason: reifies list constructor type arguments from type-checker annotations
