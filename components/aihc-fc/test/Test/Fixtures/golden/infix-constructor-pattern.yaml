extensions: []
modules:
  - |
    module Test where

    data Pair a = a :*: a

    infix 6 :*:

    first :: Pair a -> a
    first (value :*: _) = value
expected: |-
  module Test where

  data Pair (a : TYPE BoxedRep Lifted)
   = :*: (a) (a)

  first : ∀ (a : TYPE BoxedRep Lifted). (Pair a) → a =
    Λ(a : TYPE BoxedRep Lifted).
      λ(x1001 : Pair a).
        case x1001 as (_scrut : Pair a) of {
          :*: (value : (a : TYPE BoxedRep Lifted)) (_ : (a : TYPE BoxedRep Lifted)) →
            value
        }
status: pass
reason: arbitrary infix constructor patterns retain their field binders and types during FC desugaring
