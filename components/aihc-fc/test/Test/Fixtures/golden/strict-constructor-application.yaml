extensions: []
modules:
  - |
    module Test where

    data StrictPair a = StrictPair !a !a

    makePair :: a -> a -> StrictPair a
    makePair left right = StrictPair left right
expected: |-
  data StrictPair (a : TYPE BoxedRep Lifted)
   = StrictPair (a) (a)

  makePair : ∀ (a : TYPE BoxedRep Lifted). a → a → StrictPair a =
    Λ(a : TYPE BoxedRep Lifted).
      λ(x1001 : a).
        λ(y1002 : a).
          case x1001 as (_strict_field1004 : (a : TYPE BoxedRep Lifted)) of {
            _ →
              case y1002 as (_strict_field1005 : (a : TYPE BoxedRep Lifted)) of {
                _ →
                  (((Test.StrictPair : ∀ (a : TYPE BoxedRep Lifted). a → a → StrictPair a) @(a : TYPE BoxedRep Lifted)) _strict_field1004) _strict_field1005
              }
          }
status: pass
reason: saturated applications force source-strict constructor fields before allocating the constructor
