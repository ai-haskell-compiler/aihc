extensions: []
modules:
  - |
    module Test where

    data StrictPair a = StrictPair !a !a

    makePair :: a -> a -> StrictPair a
    makePair left right = StrictPair left right
expected: |
  data StrictPair a
   = StrictPair a a

  makePair : ∀ a. a → a → StrictPair a =
    Λa.
      λx1001 : a.
        λy1002 : a.
          case x1001 as _strict_field1004 : a of
            _ →
              case y1002 as _strict_field1005 : a of
                _ →
                  StrictPair @a _strict_field1004 _strict_field1005
status: pass
reason: saturated applications force source-strict constructor fields before allocating the constructor
