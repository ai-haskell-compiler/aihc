expected: |-
  prefix 1 = Test

  1.fork# : ∀ a. (State# RealWorld → (tycon "(#,#)" / 2 :: TYPE TupleRep [] → Type → TYPE TupleRep [TupleRep [], LiftedRep]) (State# RealWorld) a) → State# RealWorld → (tycon "(#,#)" / 2 :: TYPE TupleRep [] → TYPE UnliftedRep → TYPE TupleRep [TupleRep [], UnliftedRep]) (State# RealWorld) ThreadId#;
  1.yield# : State# RealWorld → State# RealWorld;

  data State# where;

  data RealWorld where;

  data ThreadId# where;

  foreign import prim fork# : ∀ a. (State# RealWorld → (tycon "(#,#)" / 2 :: TYPE TupleRep [] → Type → TYPE TupleRep [TupleRep [], LiftedRep]) (State# RealWorld) a) → State# RealWorld → (tycon "(#,#)" / 2 :: TYPE TupleRep [] → TYPE UnliftedRep → TYPE TupleRep [TupleRep [], UnliftedRep]) (State# RealWorld) ThreadId# arity 2;

  foreign import prim yield# : State# RealWorld → State# RealWorld arity 1;

  spawn : ∀ a. (State# RealWorld → (tycon "(#,#)" / 2 :: TYPE TupleRep [] → Type → TYPE TupleRep [TupleRep [], LiftedRep]) (State# RealWorld) a) → State# RealWorld → (tycon "(#,#)" / 2 :: TYPE TupleRep [] → TYPE UnliftedRep → TYPE TupleRep [TupleRep [], UnliftedRep]) (State# RealWorld) ThreadId# =
    Λ a →
      λ x1003 : State# RealWorld → (tycon "(#,#)" / 2 :: TYPE TupleRep [] → Type → TYPE TupleRep [TupleRep [], LiftedRep]) (State# RealWorld) a →
        λ y1004 : State# RealWorld →
          (1.fork# @a) x1003 y1004;

  reschedule : State# RealWorld → State# RealWorld =
    λ x1007 : State# RealWorld →
      1.yield# x1007;
extensions:
- ForeignFunctionInterface
- GHCForeignImportPrim
- MagicHash
- UnboxedTuples
modules:
- |
  module Test where

  data State# s
  data RealWorld
  data ThreadId#

  foreign import prim fork# ::
    (State# RealWorld -> (# State# RealWorld, a #)) ->
    State# RealWorld ->
    (# State# RealWorld, ThreadId# #)

  foreign import prim yield# :: State# RealWorld -> State# RealWorld

  spawn action state = fork# action state

  reschedule state = yield# state
reason: scheduler primitives retain their zero-width state and unlifted thread identity
  types through FC desugaring
status: pass
