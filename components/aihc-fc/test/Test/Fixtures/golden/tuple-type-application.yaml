extensions: []
modules:
  - |
    module Test where
    data Bool = True | False

    pair :: (Bool, ())
    pair = (True, ())
expected: |-
  data Bool
   = True
   | False

  pair : Tuple2 Bool Unit =
    (((((,) : ∀ (a : TYPE BoxedRep Lifted) (b : TYPE BoxedRep Lifted). a → b → Tuple2 a b) @Bool) @Unit) (Test.True : Bool)) (() : Unit)
status: pass
reason: reifies tuple constructor type arguments from type-checker annotations
