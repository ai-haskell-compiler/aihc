extensions: []
modules:
  - |
    module Test where
    data Bool = True | False

    pair :: (Bool, ())
    pair = (True, ())
expected: |-
  core 2
  prefix 1 = Test

  "()" : Unit;
  "(,)" : ∀ a. ∀ b. a → b → Tuple2 a b;
  1.True : Bool;

  data Bool where
    | True
    | False;

  pair : Tuple2 Bool Unit =
    (var "(,)" @Bool @Unit) 1.True var "()";
status: pass
reason: reifies tuple constructor type arguments from type-checker annotations
