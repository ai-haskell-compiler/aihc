extensions: []
modules:
  - |
    module Test where
    data Bool = True | False

    pair :: (Bool, ())
    pair = (True, ())
expected: |-
  "()" : Unit;
  "(,)" : ∀ a. ∀ b. a → b → Tuple2 a b;

  data Bool where
    | True :: Bool
    | False :: Bool;

  pair : Tuple2 Bool Unit =
    (var "(,)" @Bool @Unit) True var "()";
status: pass
reason: reifies tuple constructor type arguments from type-checker annotations
