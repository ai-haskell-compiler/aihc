extensions: []
modules:
  - |
    module Test where
    data Bool = True | False

    pair :: (Bool, ())
    pair = (True, ())
expected: |-
  module Test where

  external GHC.Tuple.() : Unit

  external GHC.Tuple.(,) : ∀ (a : TYPE BoxedRep Lifted) (b : TYPE BoxedRep Lifted). a → b → Tuple2 a b

  data Bool
   = True
   | False

  pair : Tuple2 Bool Unit =
    (((GHC.Tuple.(,) @Bool) @Unit) True) GHC.Tuple.()
status: xfail
reason: reifies tuple constructor type arguments from type-checker annotations
