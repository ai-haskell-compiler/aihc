extensions: []
modules:
  - |
    module Test where
    data Bool = True | False

    pair :: (Bool, ())
    pair = (True, ())
expected: |-
  core 2
  prefix 1 = Test

  data Bool where
    | True
    | False;

  pair : Tuple2 Bool Unit =
    (("(,)" : ∀ a. ∀ b. a → b → Tuple2 a b) @Bool @Unit) (1.True : Bool) ("()" : Unit);
status: pass
reason: reifies tuple constructor type arguments from type-checker annotations
