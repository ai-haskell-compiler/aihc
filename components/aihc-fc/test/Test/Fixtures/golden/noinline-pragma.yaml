extensions: []
modules:
  - |
    module Test where
    id x = x
    {-# NOINLINE id #-}
expected: |-
  scope 1 = "" Test

  module 1.Test where

  rec {
    id{unique 1000} : ∀ (a{unique 24} : Type). (a{unique 24} : Type) → (a{unique 24} : Type);
    id{unique 1000} =
      Λ(a{unique 24} : Type).
        λ(x1001{unique 1001} : (a{unique 24} : Type)).
          (x1001{unique 1001} : (a{unique 24} : Type))
  }
status: pass
reason: NOINLINE is accepted and ignored before System FC desugaring
