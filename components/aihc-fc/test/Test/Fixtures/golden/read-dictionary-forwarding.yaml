expected: |-
  data Result
   = Result

  data Read (a : TYPE BoxedRep Lifted)
   = $Dict$Read (Result → a)

  reads : ∀ (a : TYPE BoxedRep Lifted). (Read a) → Result → a =
    Λ(a : TYPE BoxedRep Lifted).
      λ($d0 : Read a).
        case $d0 as ($dict : Read a) of {
          $Dict$Read ($method0 : Result → a) →
            $method0
        }

  parse : ∀ (a : TYPE BoxedRep Lifted). (Read a) → Result → a =
    Λ(a : TYPE BoxedRep Lifted).
      λ($d0 : Read a).
        ((Test.reads : ∀ (a : TYPE BoxedRep Lifted). (Read a) → Result → a) @(a : TYPE BoxedRep Lifted)) $d0
extensions: []
modules:
- |
  module Test where

  data Result = Result

  class Read a where
    reads :: Result -> a

  parse :: Read a => Result -> a
  parse = reads
reason: forwards a supplied Read dictionary
status: pass
