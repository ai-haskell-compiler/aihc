expected: |-
  core 2
  prefix 1 = Test

  data Bool where
    | True
    | False;

  id : ∀ a. a → a =
    Λ a →
      λ x1001 : a →
        x1001;

  true : Bool =
    ((1.id : ∀ a. a → a) @Bool) (1.True : Bool);
extensions: []
modules:
- |
  module Test where
  data Bool = True | False

  id x = x

  true :: Bool
  true = id True
reason: reifies type arguments when desugaring polymorphic variable occurrences
status: pass
