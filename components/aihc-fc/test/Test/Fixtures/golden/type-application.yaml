expected: |-
  prefix 1 = Test

  1.True : Bool;
  1.id : ∀ a. a → a;

  data Bool where
    | True
    | False;

  id : ∀ a. a → a =
    Λ a →
      λ x1001 : a →
        x1001;

  true : Bool =
    (1.id @Bool) 1.True;
extensions: []
modules:
- |
  module Test where
  data Bool = True | False

  id x = x

  true :: Bool
  true = id True
reason: reifies type arguments when desugaring polymorphic variable occurrences
status: pass
