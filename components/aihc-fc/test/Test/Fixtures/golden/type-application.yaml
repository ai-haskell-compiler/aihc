expected: |-
  data Bool
   = True
   | False

  id : ∀ a. a → a =
    Λa.
      λx1001 : a.
        x1001

  true : Bool =
    id @Bool True
extensions: []
modules:
- |
  module Test where
  data Bool = True | False

  id x = x

  true :: Bool
  true = id True
reason: reifies type arguments when desugaring polymorphic variable occurrences
status: pass
