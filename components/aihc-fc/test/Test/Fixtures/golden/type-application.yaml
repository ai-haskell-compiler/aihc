expected: |-
  data Bool where
    | True :: Bool
    | False :: Bool;

  id : ∀ a. a → a =
    Λ a →
      λ x1001 : a →
        x1001;

  true : Bool =
    (id @Bool) True;
extensions: []
modules:
- |
  module Test where
  data Bool = True | False

  id x = x

  true :: Bool
  true = id True
reason: reifies type arguments when desugaring polymorphic variable occurrences
status: pass
