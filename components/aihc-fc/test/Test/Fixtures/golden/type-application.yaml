expected: |-
  data Bool
   = True
   | False

  id : ∀ (a : TYPE BoxedRep Lifted). a → a =
    Λ(a : TYPE BoxedRep Lifted).
      λ(x1001 : a).
        x1001

  true : Bool =
    ((Test.id : ∀ (a : TYPE BoxedRep Lifted). a → a) @Bool) (Test.True : Bool)
extensions: []
modules:
- |
  module Test where
  data Bool = True | False

  id x = x

  true :: Bool
  true = id True
reason: reifies type arguments when desugaring polymorphic variable occurrences
status: pass
