expected: |-
  core 2
  data Result where
    | Result;

  data Render a where
    | $Dict$Render : a → Result;

  render : ∀ a. Render a → a → Result =
    Λ a →
      λ $d0 : Render a →
        case $d0 as $dict : Render a of {
          $Dict$Render ($method0 : a → Result) →
            $method0;
        };

  data Convert a where
    | $Dict$Convert : ∀ b. Render b → a → b → Result;

  convert : ∀ a. ∀ b. Convert a → Render b → a → b → Result =
    Λ a →
      Λ b →
        λ $d0 : Convert a →
          λ $d1 : Render b →
            case $d0 as $dict : Convert a of {
              $Dict$Convert ($method0 : ∀ b. Render b → a → b → Result) →
                ($method0 @b) $d1;
            };
extensions: []
modules:
- |
  module Test where

  data Result = Result

  class Render a where
    render :: a -> Result

  class Convert a where
    convert :: Render b => a -> b -> Result
reason: class dictionaries store constrained polymorphic methods as ordinary function
  fields
status: pass
