expected: |-
  data Integer
   = IS (Int#)

  data Num (a : TYPE BoxedRep Lifted)
   = $Dict$Num (Integer → a)

  fromInteger : ∀ (a : TYPE BoxedRep Lifted). (Num a) → Integer → a =
    Λ(a : TYPE BoxedRep Lifted).
      λ($d0 : Num a).
        case $d0 as ($dict : Num a) of {
          $Dict$Num ($method0 : Integer → a) →
            $method0
        }
  one : ∀ (a : TYPE BoxedRep Lifted). (Num a) → a =
    Λ(a : TYPE BoxedRep Lifted).
      λ($d0 : Num a).
        (((fromInteger : ∀ (a : TYPE BoxedRep Lifted). (Num a) → Integer → a) @a) $d0) ((IS : Int# → Integer) 1#IntRep)
extensions:
- MagicHash
modules:
- |
  module GHC.Num where
  data Integer = IS Int#
  class Num a where
    fromInteger :: Integer -> a
- |
  module Main where
  one = 1
reason: desugars default integer literals through GHC.Num.fromInteger applied to an
  Integer constructor value
status: pass
