expected: |-
  core 2
  prefix 1 = Test

  1.awaitIO# : Addr# → State# RealWorld → State# RealWorld;

  data State# where;

  data RealWorld where;

  foreign import prim awaitIO# : Addr# → State# RealWorld → State# RealWorld arity 2;

  await : Addr# → State# RealWorld → State# RealWorld =
    λ x1002 : Addr# →
      λ y1003 : State# RealWorld →
        1.awaitIO# x1002 y1003;
extensions:
- ForeignFunctionInterface
- GHCForeignImportPrim
- MagicHash
modules:
- |
  module Test where

  data State# s
  data RealWorld

  foreign import prim awaitIO# ::
    Addr# -> State# RealWorld -> State# RealWorld

  await request state = awaitIO# request state
reason: the compiler exposes one operation-independent IO suspension primitive
  while concrete operations remain runtime calls
status: pass
