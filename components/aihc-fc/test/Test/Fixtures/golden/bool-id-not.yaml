expected: |-
  core 2
  prefix 1 = Test

  1.False : Bool;
  1.True : Bool;

  data Bool where
    | True
    | False;

  id : ∀ a. a → a =
    Λ a →
      λ x1001 : a →
        x1001;

  not : Bool → Bool =
    λ x1003 : Bool →
      case x1003 as _scrut : Bool of {
        True →
          1.False;
        False →
          1.True;
      };
extensions: []
modules:
- |
  module Test where
  data Bool = True | False
  id x = x
  not True = False
  not False = True
reason: 'MVP: data Bool + id + not desugared to System FC'
status: pass
