extensions: []
modules:
  - |
    module Test where
    data Bool = True | False
    id x = x
    not True = False
    not False = True
expected: |
  data Bool
   = True
   | False

  id : ∀ a. a → a =
    Λa.
      λx : a.
        x

  not : Bool → Bool =
    λx : Bool.
      case x as _scrut : Bool of
        True →
          False
        False →
          True
status: pass
reason: "MVP: data Bool + id + not desugared to System FC"
