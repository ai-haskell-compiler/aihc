expected: |-
  module Test where

  data State#

  data MutVar#

  foreign prim newMutVar#/2 : ∀ (rep : RuntimeRep) (value : TYPE rep) (domain : TYPE BoxedRep Lifted). value → (State# domain) → (#,#) (State# domain) (MutVar# domain value)
extensions:
- ExplicitForAll
- ForeignFunctionInterface
- GHCForeignImportPrim
- KindSignatures
- MagicHash
- UnboxedTuples
modules:
- |
  module Test where

  import GHC.Types (RuntimeRep, TYPE)

  data State# s
  data MutVar# d a

  foreign import prim newMutVar# ::
    forall (rep :: RuntimeRep) (value :: TYPE rep) domain.
    value -> State# domain -> (# State# domain, MutVar# domain value #)
reason: primitive import type matching rejects explicit runtime representation and value variables
status: xfail
