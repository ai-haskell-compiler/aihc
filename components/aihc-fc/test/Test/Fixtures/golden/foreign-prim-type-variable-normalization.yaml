expected: |-
  core 2
  data State# where;

  data MutVar# where;

  foreign import prim newMutVar# : ∀ value. ∀ domain. value → State# domain → (tycon "(#,#)" / 2 :: TYPE TupleRep [] → TYPE UnliftedRep → TYPE TupleRep [TupleRep [], UnliftedRep]) (State# domain) (MutVar# domain value) arity 2;
extensions:
- ForeignFunctionInterface
- GHCForeignImportPrim
- MagicHash
- UnboxedTuples
modules:
- |
  module Test where

  data State# s
  data MutVar# d a

  foreign import prim newMutVar# ::
    value -> State# domain -> (# State# domain, MutVar# domain value #)
reason: primitive import types match after normalizing quantified variable names
status: pass
