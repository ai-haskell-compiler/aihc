expected: |-
  data Bool
   = False
   | True

  select : ∀ (a : TYPE BoxedRep Lifted). (a → Bool) → [a] → [a] =
    Λ(a : TYPE BoxedRep Lifted).
      λ(x1001 : a → Bool).
        λ(y1002 : [a]).
          let {
            rec {
              $lc1003 : [a] → [a];
              $lc1003 =
                λ(_lc_list1004 : [a]).
                  case _lc_list1004 as (_lc_scrut1007 : [a]) of {
                    [] →
                      ([] : ∀ (a : TYPE BoxedRep Lifted). [a]) @a;
                    : (_lc_head1005 : a) (_lc_tail1006 : [a]) →
                      case x1001 _lc_head1005 as (_lc_guard1008 : Bool) of {
                        True →
                          (((: : ∀ (a : TYPE BoxedRep Lifted). a → [a] → [a]) @a) _lc_head1005) ($lc1003 _lc_tail1006);
                        False →
                          $lc1003 _lc_tail1006
                      }
                  }
            }
          } in
            $lc1003 y1002
extensions: []
modules:
- |
  module Test where
  data Bool = False | True

  select fn list = [x | x <- list, fn x]
reason: desugars list comprehensions directly to recursive Core without Prelude helpers
status: pass
