expected: |-
  ":" : ∀ a. a → tycon "[]" a → tycon "[]" a;
  "[]" : ∀ a. tycon "[]" a;

  data Bool where
    | False :: Bool
    | True :: Bool;

  select : ∀ a. (a → Bool) → tycon "[]" a → tycon "[]" a =
    Λ a →
      λ x1001 : a → Bool →
        λ y1002 : tycon "[]" a →
          let rec {
            $lc1003 : tycon "[]" a → tycon "[]" a =
              λ _lc_list1004 : tycon "[]" a →
                case _lc_list1004 as _lc_scrut1007 : tycon "[]" a of {
                  "[]" →
                    var "[]" @a;
                  ":" (_lc_head1005 : a) (_lc_tail1006 : tycon "[]" a) →
                    case x1001 _lc_head1005 as _lc_guard1008 : Bool of {
                      True →
                        (var ":" @a) _lc_head1005 ($lc1003 _lc_tail1006);
                      False →
                        $lc1003 _lc_tail1006;
                    };
                };
          }
          in
            $lc1003 y1002;
extensions: []
modules:
- |
  module Test where
  data Bool = False | True

  select fn list = [x | x <- list, fn x]
reason: desugars list comprehensions directly to recursive Core without Prelude helpers
status: pass
