expected: |-
  data Bool
   = False
   | True

  data Eq (a : TYPE BoxedRep Lifted)
   = $Dict$Eq (a → a → Bool)

  == : ∀ (a : TYPE BoxedRep Lifted). (Eq a) → a → a → Bool =
    Λ(a : TYPE BoxedRep Lifted).
      λ($d0 : Eq a).
        case $d0 as ($dict : Eq a) of {
          $Dict$Eq ($method0 : a → a → Bool) →
            $method0
        }

  $fEqBool : Eq Bool =
    (($Dict$Eq : ∀ (a : TYPE BoxedRep Lifted). (a → a → Bool) → Eq a) @Bool) (λ(x1004 : Bool).
      λ(y1005 : Bool).
        case x1004 as (_scrut : Bool) of {
          False →
            case y1005 as (_scrut : Bool) of {
              False →
                (Test.True : Bool);
              True →
                (Test.False : Bool)
            };
          True →
            case y1005 as (_scrut : Bool) of {
              False →
                (Test.False : Bool);
              True →
                (Test.True : Bool)
            }
        })

  eqBool : Bool → Bool → Bool =
    ((Test.== : ∀ (a : TYPE BoxedRep Lifted). (Eq a) → a → a → Bool) @Bool) ($fEqBool : Eq Bool)
extensions: []
modules:
- |
  module Test where
  data Bool = False | True

  class Eq a where
    (==) :: a -> a -> Bool

  instance Eq Bool where
    False == False = True
    False == True = False
    True == False = False
    True == True = True

  eqBool :: Bool -> Bool -> Bool
  eqBool = (==)
reason: reifies both type arguments and dictionaries for class method occurrences
status: pass
