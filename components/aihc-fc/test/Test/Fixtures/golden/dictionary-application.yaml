expected: |-
  core 2
  prefix 1 = Test

  data Bool where
    | False
    | True;

  data Eq a where
    | $Dict$Eq : a → a → Bool;

  "==" : ∀ a. Eq a → a → a → Bool =
    Λ a →
      λ $d0 : Eq a →
        case $d0 as $dict : Eq a of {
          $Dict$Eq ($method0 : a → a → Bool) →
            $method0;
        };

  $fEqBool : Eq Bool =
    (($Dict$Eq : ∀ a. (a → a → Bool) → Eq a) @Bool) (λ x1004 : Bool →
      λ y1005 : Bool →
        case x1004 as _scrut : Bool of {
          False →
            case y1005 as _scrut : Bool of {
              False →
                (1.True{1006} : Bool);
              True →
                (1.False{1007} : Bool);
            };
          True →
            case y1005 as _scrut : Bool of {
              False →
                (1.False{1009} : Bool);
              True →
                (1.True{1010} : Bool);
            };
        });

  eqBool : Bool → Bool → Bool =
    ((1."==" : ∀ a. Eq a → a → a → Bool) @Bool) ($fEqBool : Eq Bool);
extensions: []
modules:
- |
  module Test where
  data Bool = False | True

  class Eq a where
    (==) :: a -> a -> Bool

  instance Eq Bool where
    False == False = True
    False == True = False
    True == False = False
    True == True = True

  eqBool :: Bool -> Bool -> Bool
  eqBool = (==)
reason: reifies both type arguments and dictionaries for class method occurrences
status: pass
