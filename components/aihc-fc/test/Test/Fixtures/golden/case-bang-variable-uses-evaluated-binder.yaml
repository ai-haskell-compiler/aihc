extensions:
  - BangPatterns
expected: |-
  scope 1 = "" Test

  module 1.Test where

  data 1.Box : Type
   = 1.Box

  rec {
    forceBox{unique 1000} : tycon 1.Box/0 { :: Type } → tycon 1.Box/0 { :: Type };
    forceBox{unique 1000} =
      λ(x1001{unique 1001} : tycon 1.Box/0 { :: Type }).
        case (x1001{unique 1001} : tycon 1.Box/0 { :: Type }) as (_bang1002{unique 1002} : tycon 1.Box/0 { :: Type }) return (tycon 1.Box/0 { :: Type }) of {
          _ →
            (_bang1002{unique 1002} : tycon 1.Box/0 { :: Type })
        }
  }
modules:
  - |
    module Test where

    data Box = Box

    forceBox :: Box -> Box
    forceBox value =
      case value of
        !evaluated -> evaluated
status: pass
reason: a strict variable pattern binds its RHS to the evaluated Core case binder
