extensions: []
modules:
  - |
    module Test where
    first x = shared x
    second x = shared x
    shared x = x
expected: |
  module Test where

  rec {
    first{unique 1000} : ∀ (a{unique 35} : TYPE BoxedRep Lifted). a → a;
    second{unique 1001} : ∀ (a{unique 27} : TYPE BoxedRep Lifted). a → a;
    shared{unique 1002} : ∀ (a{unique 19} : TYPE BoxedRep Lifted). a → a;
    first{unique 1000} =
      Λ(a{unique 35} : TYPE BoxedRep Lifted).
        λ(x1003{unique 1003} : a).
          (shared{unique 1002} @a) x1003{unique 1003};
    second{unique 1001} =
      Λ(a{unique 27} : TYPE BoxedRep Lifted).
        λ(x1004{unique 1004} : a).
          (shared{unique 1002} @a) x1004{unique 1004};
    shared{unique 1002} =
      Λ(a{unique 19} : TYPE BoxedRep Lifted).
        λ(x1005{unique 1005} : a).
          x1005{unique 1005}
  }
status: pass
reason: all uses of a top-level binding have the same System FC variable identity
