extensions: []
modules:
  - |
    module Test where
    first x = shared x
    second x = shared x
    shared x = x
expected: |-
  module "" Test where

  rec {
    first{unique 1000} : ∀ (a{unique 40} : Type). (a{unique 40} : Type) → (a{unique 40} : Type);
    second{unique 1001} : ∀ (a{unique 32} : Type). (a{unique 32} : Type) → (a{unique 32} : Type);
    shared{unique 1002} : ∀ (a{unique 24} : Type). (a{unique 24} : Type) → (a{unique 24} : Type);
    first{unique 1000} =
      Λ(a{unique 40} : Type).
        λ(x1003{unique 1003} : (a{unique 40} : Type)).
          ((shared{unique 1002} : ∀ (a{unique 24} : Type). (a{unique 24} : Type) → (a{unique 24} : Type)) @(a{unique 40} : Type)) (x1003{unique 1003} : (a{unique 40} : Type));
    second{unique 1001} =
      Λ(a{unique 32} : Type).
        λ(x1004{unique 1004} : (a{unique 32} : Type)).
          ((shared{unique 1002} : ∀ (a{unique 24} : Type). (a{unique 24} : Type) → (a{unique 24} : Type)) @(a{unique 32} : Type)) (x1004{unique 1004} : (a{unique 32} : Type));
    shared{unique 1002} =
      Λ(a{unique 24} : Type).
        λ(x1005{unique 1005} : (a{unique 24} : Type)).
          (x1005{unique 1005} : (a{unique 24} : Type))
  }
status: pass
reason: all uses of a top-level binding have the same System FC variable identity
