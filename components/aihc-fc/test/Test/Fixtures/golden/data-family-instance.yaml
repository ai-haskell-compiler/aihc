extensions:
  - TypeFamilies
modules:
  - |
    module Test where
    data family Payload a
    data instance Payload a = PayloadValue a
expected: |-
  data $R$Payload$PayloadValue a where
    | PayloadValue :: ∀ a. a → tycon $R$Payload$PayloadValue a;

  axiom $ax$Payload$PayloadValue a nominal : Payload a ~ tycon $R$Payload$PayloadValue a;
status: pass
reason: data-family instances retain an explicit representation type and nominal equality axiom in System FC
