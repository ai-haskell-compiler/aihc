expected: |-
  scope 1 = "" Test

  module 1.Test where

  rec {
    id{unique 1000} : ∀ (a{unique 22} : Type). (a{unique 22} : Type) → (a{unique 22} : Type);
    id{unique 1000} =
      Λ(a{unique 22} : Type).
        λ(x1001{unique 1001} : (a{unique 22} : Type)).
          (x1001{unique 1001} : (a{unique 22} : Type))
  }
extensions: []
modules:
- |
  module Test where
  id = \x -> x
reason: desugars inferred polymorphic lambda binder types from type-checker annotations
status: pass
