extensions:
  - TypeFamilies
modules:
  - |
    module Test where
    data Unit = Unit
    data family Wrapped a
    newtype instance Wrapped Unit = WrappedUnit Unit
expected: |-
  data Unit where
    | Unit;

  newtype $R$Wrapped$WrappedUnit = WrappedUnit Unit represents tycon $R$Wrapped$WrappedUnit;

  axiom $ax$Wrapped$WrappedUnit nominal : Wrapped Unit ~ tycon $R$Wrapped$WrappedUnit;
status: pass
reason: newtype-family instances retain a representation newtype and their nominal family equation
