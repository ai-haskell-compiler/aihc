extensions: []
modules:
  - |
    module Test where
    a + b = (a,b)
    a * b = (a,b)
    infixl 6 +
    infixl 7 *
output: |
  (('a','b'),('c','d'))
expression: |
  'a' * 'b' + 'c' * 'd'
status: pass
reason: groups higher-precedence operators on both sides of a lower-precedence operator
