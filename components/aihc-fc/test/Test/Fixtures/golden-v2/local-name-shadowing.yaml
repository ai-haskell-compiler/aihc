extensions: []
modules:
  - |
    module Test where
    choose = \a -> (\a -> a) a
    reuse = \a -> (\a -> a) ((\a -> a) a)
expected: |-
  scope 1 = "" Test
  scope 2 = "aihc-prim" GHC.Types

  module 1.Test where

  pub val 1.vchoose :: ∀(a : 2.tType). a → a
   = Λ(a : 2.tType).
    λ(a : a).
      (λ(a{1} : a).
        a{1}) a

  pub val 1.vreuse :: ∀(a : 2.tType). a → a
   = Λ(a : 2.tType).
    λ(a : a).
      (λ(a{1} : a).
        a{1}) ((λ(a{1} : a).
        a{1}) a)
status: pass
reason: Local names use the lowest number in each lexical scope.
