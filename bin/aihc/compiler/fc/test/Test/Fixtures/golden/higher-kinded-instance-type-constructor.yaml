extensions:
  - KindSignatures
modules:
  - |
    module Test where
    import GHC.Types (Type)
    data Box a = Box a
    class Map (f :: Type -> Type) where
      mapValue :: f a -> f a
    instance Map Box where
      mapValue value = value
expected: |-
  scope 1 = "" Test
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.sType;
    2.sLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.sType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.sType;
    2.sType :: 2.sType;
    2.cBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.cLifted :: 2.tLevity

  import synonyms
    2.sLiftedRep = 2.cBoxedRep 2.cLifted;
    2.sType = 2.tTYPE 2.sLiftedRep

  pub type 1.tBox (a : 2.sType) :: 2.sType {
      pub 1.cBox :: ∀(a : 2.sType). a 2.→ 1.tBox a
  }

  pub type 1.t$Dict$Map (f : 2.sType 2.→ 2.sType) :: 2.sType {
      pub 1.c$Dict$Map :: ∀(f : 2.sType 2.→ 2.sType). (∀(a : 2.sType). f a 2.→ f a) 2.→ 1.t$Dict$Map f
  }

  pub val 1.vmapValue :: ∀(f : 2.sType 2.→ 2.sType) (a : 2.sType). 1.t$Dict$Map f 2.→ f a 2.→ f a
   = Λ(f : 2.sType 2.→ 2.sType).
    Λ(a : 2.sType).
      λ($d0 : 1.t$Dict$Map f).
        case $d0 as ($dict : 1.t$Dict$Map f) return (f a 2.→ f a) of {
            1.c$Dict$Map ($method0 : ∀(a{1} : 2.sType). f a{1} 2.→ f a{1}) →
                $method0 @a
        }

  pub val 1.v$fMapBox :: 1.t$Dict$Map 1.tBox
   = 1.c$Dict$Map @1.tBox 1.v$fMapBox$cmapValue

  val 1.v$fMapBox$cmapValue :: ∀(a : 2.sType). 1.tBox a 2.→ 1.tBox a
   = Λ(a : 2.sType).
    λ(x : 1.tBox a).
      x
status: pass
reason: ''
