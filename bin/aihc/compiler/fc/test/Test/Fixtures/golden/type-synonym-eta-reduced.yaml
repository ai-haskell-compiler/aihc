---
extensions: []
modules:
- |
  module Test where

  data Box a = Box a
  type Wrapped = Box

  unbox :: Wrapped a -> a
  unbox (Box x) = x
expected: |-
  scope 1 = "" Test
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.sType;
    2.sLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.sType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.sType;
    2.sType :: 2.sType;
    2.cBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.cLifted :: 2.tLevity

  import synonyms
    2.sLiftedRep = 2.cBoxedRep 2.cLifted;
    2.sType = 2.tTYPE 2.sLiftedRep

  pub type 1.tBox (a : 2.sType) :: 2.sType {
      pub 1.cBox :: ∀(a : 2.sType). a 2.→ 1.tBox a
  }

  pub type 1.sWrapped :: ∀(eta0 : 2.sType). 2.sType =
   1.tBox

  pub val 1.vunbox :: ∀(a : 2.sType). 1.tBox a 2.→ a
   = Λ(a : 2.sType).
    λ(x : 1.tBox a).
      case x as (_scrut : 1.tBox a) return (a) of {
          1.cBox (x{1} : a) →
              x{1}
      }
status: pass
reason: An eta-reduced synonym keeps the arrows after its parameters as the
  binders of a type constructor kind, so its body has the declared kind.
