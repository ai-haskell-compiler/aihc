---
extensions: []
modules:
- |
  module Test where

  data Result = Result

  class Read a where
    reads :: Result -> a

  parse :: Read a => Result -> a
  parse = reads
expected: |-
  scope 1 = "" Test
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.sType;
    2.sLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.sType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.sType;
    2.sType :: 2.sType;
    2.cBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.cLifted :: 2.tLevity

  import synonyms
    2.sLiftedRep = 2.cBoxedRep 2.cLifted;
    2.sType = 2.tTYPE 2.sLiftedRep

  pub type 1.tResult :: 2.sType {
      pub 1.cResult :: 1.tResult
  }

  pub type 1.t$Dict$Read (a : 2.sType) :: 2.sType {
      pub 1.c$Dict$Read :: ∀(a : 2.sType). (1.tResult 2.→ a) 2.→ 1.t$Dict$Read a
  }

  pub val 1.vreads :: ∀(a : 2.sType). 1.t$Dict$Read a 2.→ 1.tResult 2.→ a
   = Λ(a : 2.sType).
    λ($d0 : 1.t$Dict$Read a).
      case $d0 as ($dict : 1.t$Dict$Read a) return (1.tResult 2.→ a) of {
          1.c$Dict$Read ($method0 : 1.tResult 2.→ a) →
              $method0
      }

  pub val 1.vparse :: ∀(a : 2.sType). 1.t$Dict$Read a 2.→ 1.tResult 2.→ a
   = Λ(a : 2.sType).
    λ($d0 : 1.t$Dict$Read a).
      1.vreads @a $d0
status: pass
reason: The function forwards a supplied Read dictionary.
