---
extensions: []
modules:
- |
  module Main where
  import GHC.Prim.Base (Monad, return)
  identityAction :: Monad m => m a -> m a
  identityAction action = do
    value <- action
    return value
expected: |-
  scope 1 = "" Main
  scope 2 = "aihc-prim" GHC.Prim.Base
  scope 3 = "aihc-prim" GHC.Types

  import headers
    2.t$Dict$Monad :: ∀(m : 3.sType 3.→ 3.sType). 3.sType;
    3.tLevity :: 3.sType;
    3.sLiftedRep :: 3.tRuntimeRep;
    3.tRuntimeRep :: 3.sType;
    3.tTYPE :: ∀(rep : 3.tRuntimeRep). 3.sType;
    3.sType :: 3.sType;
    2.v>>= :: ∀(m : 3.sType 3.→ 3.sType) (a : 3.sType) (b : 3.sType). 2.t$Dict$Monad m 3.→ m a 3.→ (a 3.→ m b) 3.→ m b;
    3.cBoxedRep :: 3.tLevity 3.→ 3.tRuntimeRep;
    3.cLifted :: 3.tLevity;
    2.vreturn :: ∀(m : 3.sType 3.→ 3.sType) (a : 3.sType). 2.t$Dict$Monad m 3.→ a 3.→ m a

  import synonyms
    3.sLiftedRep = 3.cBoxedRep 3.cLifted;
    3.sType = 3.tTYPE 3.sLiftedRep

  pub val 1.videntityAction :: ∀(m : 3.sType 3.→ 3.sType) (a : 3.sType). 2.t$Dict$Monad m 3.→ m a 3.→ m a
   = Λ(m : 3.sType 3.→ 3.sType).
    Λ(a : 3.sType).
      λ($d0 : 2.t$Dict$Monad m).
        λ(x : m a).
          2.v>>= @m @a @a $d0 x (λ(value : a).
            2.vreturn @m @a $d0 value)
status: pass
reason: type-checks and desugars do notation with only aihc-prim built-ins
