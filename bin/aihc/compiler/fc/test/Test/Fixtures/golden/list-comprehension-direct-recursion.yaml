---
extensions: []
modules:
- |
  module Test where
  import GHC.Types (Bool)

  select fn list = [x | x <- list, fn x]
expected: ''
status: xfail
reason: FC desugaring does not support EListComp expressions.
