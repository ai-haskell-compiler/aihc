extensions: []
modules:
- |
  module Test where
  data Box a = Box a
  data Maybe a = Nothing | Just a
  local :: a -> Box a
  local x = Box x
  poly :: a -> Maybe (Maybe a)
  poly _ = Just Nothing
passes:
- lift-constants
expected: |-
  scope 1 = "" Test
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.sType;
    2.sLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.sType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.sType;
    2.sType :: 2.sType;
    2.cBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.cLifted :: 2.tLevity

  import synonyms
    2.sLiftedRep = 2.cBoxedRep 2.cLifted;
    2.sType = 2.tTYPE 2.sLiftedRep

  pub type 1.tBox (a : 2.sType) :: 2.sType {
      pub 1.cBox :: ∀(a : 2.sType). a 2.→ 1.tBox a
  }

  pub type 1.tMaybe (a : 2.sType) :: 2.sType {
      pub 1.cNothing :: ∀(a : 2.sType). 1.tMaybe a;
      pub 1.cJust :: ∀(a : 2.sType). a 2.→ 1.tMaybe a
  }

  pub val 1.vlocal :: ∀(a : 2.sType). a 2.→ 1.tBox a
   = Λ(a : 2.sType).
    λ(x : a).
      1.cBox @a x

  pub val 1.vpoly :: ∀(a : 2.sType). a 2.→ 1.tMaybe (1.tMaybe a)
   = Λ(a : 2.sType).
    λ(x : a).
      1.cJust @(1.tMaybe a) (1.cNothing @a)
status: pass
reason: Local term and type references prevent constant lifting.
