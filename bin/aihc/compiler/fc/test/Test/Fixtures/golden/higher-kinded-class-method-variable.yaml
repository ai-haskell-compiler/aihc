extensions:
  - KindSignatures
modules:
  - |
    module Test where
    import GHC.Types (Type)
    data Token = Token
    class Supply (a :: Type) where
      supply :: p a -> Token
expected: |-
  scope 1 = "" Test
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.sType;
    2.sLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.sType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.sType;
    2.sType :: 2.sType;
    2.cBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.cLifted :: 2.tLevity

  import synonyms
    2.sLiftedRep = 2.cBoxedRep 2.cLifted;
    2.sType = 2.tTYPE 2.sLiftedRep

  pub type 1.tToken :: 2.sType {
      pub 1.cToken :: 1.tToken
  }

  pub type 1.t$Dict$Supply (a : 2.sType) :: 2.sType {
      pub 1.c$Dict$Supply :: ∀(a : 2.sType). (∀(p : 2.sType 2.→ 2.sType). p a 2.→ 1.tToken) 2.→ 1.t$Dict$Supply a
  }

  pub val 1.vsupply :: ∀(a : 2.sType) (p : 2.sType 2.→ 2.sType). 1.t$Dict$Supply a 2.→ p a 2.→ 1.tToken
   = Λ(a : 2.sType).
    Λ(p : 2.sType 2.→ 2.sType).
      λ($d0 : 1.t$Dict$Supply a).
        case $d0 as ($dict : 1.t$Dict$Supply a) return (p a 2.→ 1.tToken) of {
            1.c$Dict$Supply ($method0 : ∀(p{1} : 2.sType 2.→ 2.sType). p{1} a 2.→ 1.tToken) →
                $method0 @p
        }
status: pass
reason: ''
