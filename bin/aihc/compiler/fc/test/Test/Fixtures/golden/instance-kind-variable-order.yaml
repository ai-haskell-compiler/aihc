extensions:
- PolyKinds
- KindSignatures
- ScopedTypeVariables
- FlexibleInstances
modules:
- |
  module Type.Reflection.Internal where
  import GHC.Types (Type)
  data TypeRep (a :: k) = TypeRep
  class Typeable (a :: k) where
    typeRep :: TypeRep a
- |
  module Test where
  import GHC.Types (Type)
  import Type.Reflection.Internal
  data Unit = Unit
  class Binary a where
    put :: a -> Unit
  instance Typeable a => Binary (TypeRep (a :: k)) where
    put _ = Unit
expected: |-
  scope 1 = "" Type.Reflection.Internal
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.sType;
    2.sLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.sType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.sType;
    2.sType :: 2.sType;
    2.cBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.cLifted :: 2.tLevity

  import synonyms
    2.sLiftedRep = 2.cBoxedRep 2.cLifted;
    2.sType = 2.tTYPE 2.sLiftedRep

  pub type 1.tTypeRep (k : 2.sType) (a : k) :: 2.sType {
      pub 1.cTypeRep :: ∀(k : 2.sType) (a : k). 1.tTypeRep k a
  }

  pub type 1.t$Dict$Typeable (k : 2.sType) (a : k) :: 2.sType {
      pub 1.c$Dict$Typeable :: ∀(k : 2.sType) (a : k). 1.tTypeRep k a 2.→ 1.t$Dict$Typeable k a
  }

  pub val 1.vtypeRep :: ∀(k : 2.sType) (a : k). 1.t$Dict$Typeable k a 2.→ 1.tTypeRep k a
   = Λ(k : 2.sType).
    Λ(a : k).
      λ($d0 : 1.t$Dict$Typeable k a).
        case $d0 as ($dict : 1.t$Dict$Typeable k a) return (1.tTypeRep k a) of {
            1.c$Dict$Typeable ($method0 : 1.tTypeRep k a) →
                $method0
        }
  scope 1 = "" Test
  scope 2 = "" Type.Reflection.Internal
  scope 3 = "aihc-prim" GHC.Types

  import headers
    2.t$Dict$Typeable :: ∀(k : 3.sType) (a : k). 3.sType;
    3.tLevity :: 3.sType;
    3.sLiftedRep :: 3.tRuntimeRep;
    3.tRuntimeRep :: 3.sType;
    3.tTYPE :: ∀(rep : 3.tRuntimeRep). 3.sType;
    3.sType :: 3.sType;
    2.tTypeRep :: ∀(k : 3.sType) (a : k). 3.sType;
    3.cBoxedRep :: 3.tLevity 3.→ 3.tRuntimeRep;
    3.cLifted :: 3.tLevity

  import synonyms
    3.sLiftedRep = 3.cBoxedRep 3.cLifted;
    3.sType = 3.tTYPE 3.sLiftedRep

  pub type 1.tUnit :: 3.sType {
      pub 1.cUnit :: 1.tUnit
  }

  pub type 1.t$Dict$Binary (a : 3.sType) :: 3.sType {
      pub 1.c$Dict$Binary :: ∀(a : 3.sType). (a 3.→ 1.tUnit) 3.→ 1.t$Dict$Binary a
  }

  pub val 1.vput :: ∀(a : 3.sType). 1.t$Dict$Binary a 3.→ a 3.→ 1.tUnit
   = Λ(a : 3.sType).
    λ($d0 : 1.t$Dict$Binary a).
      case $d0 as ($dict : 1.t$Dict$Binary a) return (a 3.→ 1.tUnit) of {
          1.c$Dict$Binary ($method0 : a 3.→ 1.tUnit) →
              $method0
      }

  pub val 1.v$fBinaryTypeRepa :: ∀(k : 3.sType) (a : k). 2.t$Dict$Typeable k a 3.→ 1.t$Dict$Binary (2.tTypeRep k a)
   = Λ(k : 3.sType).
    Λ(a : k).
      λ($d0 : 2.t$Dict$Typeable k a).
        1.c$Dict$Binary @(2.tTypeRep k a) (1.v$fBinaryTypeRepa$cput @k @a $d0)

  val 1.v$fBinaryTypeRepa$cput :: ∀(k : 3.sType) (a : k). 2.t$Dict$Typeable k a 3.→ 2.tTypeRep k a 3.→ 1.tUnit
   = Λ(k : 3.sType).
    Λ(a : k).
      λ($d0 : 2.t$Dict$Typeable k a).
        λ(x : 2.tTypeRep k a).
          1.cUnit
status: pass
reason: an instance head that gives a parameter an implicit kind variable quantifies the kind variable before the parameter, so the dictionary type is well scoped.
