---
extensions: []
modules:
- |
  module Parent where

  data Flag = Off | On

  class Parent a where
    parent :: a -> Flag

  instance Parent Flag where
    parent value = value
- |
  module Child where

  import Parent

  class Parent a => Child a where
    child :: a -> Flag
expected: |-
  scope 1 = "" Parent
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.sType;
    2.sLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.sType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.sType;
    2.sType :: 2.sType;
    2.cBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.cLifted :: 2.tLevity

  import synonyms
    2.sLiftedRep = 2.cBoxedRep 2.cLifted;
    2.sType = 2.tTYPE 2.sLiftedRep

  pub type 1.tFlag :: 2.sType {
      pub 1.cOff :: 1.tFlag;
      pub 1.cOn :: 1.tFlag
  }

  pub type 1.t$Dict$Parent (a : 2.sType) :: 2.sType {
      pub 1.c$Dict$Parent :: ∀(a : 2.sType). (a 2.→ 1.tFlag) 2.→ 1.t$Dict$Parent a
  }

  pub val 1.vparent :: ∀(a : 2.sType). 1.t$Dict$Parent a 2.→ a 2.→ 1.tFlag
   = Λ(a : 2.sType).
    λ($d0 : 1.t$Dict$Parent a).
      case $d0 as ($dict : 1.t$Dict$Parent a) return (a 2.→ 1.tFlag) of {
          1.c$Dict$Parent ($method0 : a 2.→ 1.tFlag) →
              $method0
      }

  pub val 1.v$fParentFlag :: 1.t$Dict$Parent 1.tFlag
   = 1.c$Dict$Parent @1.tFlag 1.v$fParentFlag$cparent

  val 1.v$fParentFlag$cparent :: 1.tFlag 2.→ 1.tFlag
   = λ(x : 1.tFlag).
    x
  scope 1 = "" Child
  scope 2 = "" Parent
  scope 3 = "aihc-prim" GHC.Types

  import headers
    2.t$Dict$Parent :: ∀(a : 3.sType). 3.sType;
    2.tFlag :: 3.sType;
    3.tLevity :: 3.sType;
    3.sLiftedRep :: 3.tRuntimeRep;
    3.tRuntimeRep :: 3.sType;
    3.tTYPE :: ∀(rep : 3.tRuntimeRep). 3.sType;
    3.sType :: 3.sType;
    3.cBoxedRep :: 3.tLevity 3.→ 3.tRuntimeRep;
    3.cLifted :: 3.tLevity

  import synonyms
    3.sLiftedRep = 3.cBoxedRep 3.cLifted;
    3.sType = 3.tTYPE 3.sLiftedRep

  pub type 1.t$Dict$Child (a : 3.sType) :: 3.sType {
      pub 1.c$Dict$Child :: ∀(a : 3.sType). 2.t$Dict$Parent a 3.→ (a 3.→ 2.tFlag) 3.→ 1.t$Dict$Child a
  }

  pub val 1.vchild :: ∀(a : 3.sType). 1.t$Dict$Child a 3.→ a 3.→ 2.tFlag
   = Λ(a : 3.sType).
    λ($d0 : 1.t$Dict$Child a).
      case $d0 as ($dict : 1.t$Dict$Child a) return (a 3.→ 2.tFlag) of {
          1.c$Dict$Child ($method0 : 2.t$Dict$Parent a) ($method1 : a 3.→ 2.tFlag) →
              $method1
      }
status: pass
reason: Class constraints become value dictionary types and keep their checked source
  identity.
