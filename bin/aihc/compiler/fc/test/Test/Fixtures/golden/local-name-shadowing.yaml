extensions: []
modules:
  - |
    module Test where
    choose = \a -> (\a -> a) a
    reuse = \a -> (\a -> a) ((\a -> a) a)
expected: |-
  scope 1 = "" Test
  scope 2 = "aihc-prim" GHC.Types

  import headers
    2.tLevity :: 2.sType;
    2.sLiftedRep :: 2.tRuntimeRep;
    2.tRuntimeRep :: 2.sType;
    2.tTYPE :: ∀(rep : 2.tRuntimeRep). 2.sType;
    2.sType :: 2.sType;
    2.cBoxedRep :: 2.tLevity 2.→ 2.tRuntimeRep;
    2.cLifted :: 2.tLevity

  import synonyms
    2.sLiftedRep = 2.cBoxedRep 2.cLifted;
    2.sType = 2.tTYPE 2.sLiftedRep

  pub val 1.vchoose :: ∀(a : 2.sType). a 2.→ a
   = Λ(a : 2.sType).
    λ(a{1} : a).
      (λ(a{2} : a).
        a{2}) a{1}

  pub val 1.vreuse :: ∀(a : 2.sType). a 2.→ a
   = Λ(a : 2.sType).
    λ(a{1} : a).
      (λ(a{2} : a).
        a{2}) ((λ(a{2} : a).
        a{2}) a{1})
status: pass
reason: Local names use the lowest number in each lexical scope.
