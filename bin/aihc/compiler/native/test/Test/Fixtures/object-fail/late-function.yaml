language: lir
error: stack.alloc alignment above 16
source: |
  func @first() -> i64 {
  entry:
    return 42
  }

  func @later() -> i64 {
  entry:
    %buffer = stack.alloc 64 align 32
    return 0
  }
