expected: |-
  constructor Test.Box [IntRep]

  global "\NULTest\NULBox" = (CTest.Box/1)

  global "\NULTest\NULvalue" = (F$grin_value_thunk_0)

  $grin_value_thunk_0 -> BoxedRep Lifted =
    argument :: IntRep <-
      constant (1 :: IntRep)
    argument%1 :: WordRep <-
      constant (2 :: WordRep)
    _case :: IntRep, _case%1 :: WordRep <-
      constant (argument :: IntRep) (argument%1 :: WordRep)
    first :: IntRep, _pat :: WordRep <-
      constant (_case :: IntRep) (_case%1 :: WordRep)
    argument%2 :: IntRep <-
      constant (first :: IntRep)
    store (CTest.Box (argument%2 :: IntRep))
extensions:
- MagicHash
- UnboxedTuples
reason: returns heterogeneous unboxed-tuple components directly without allocating
  or matching a node
source: |
  module Test where
  import GHC.Prim
  data Box = Box Int#
  value :: Box
  value = Box (case (# 1#, 2## #) of (# first, _ #) -> first)
status: pass
