expected: |-
  scope 1 = "" Dependency

  constructor 1.Box [IntRep]

  global 1.Box = (C1.Box/1)

  global 1.identity = (P$grin_closure_0/1)

  global 1.caf = (C1.Box (7 :: IntRep))

  $grin_closure_0 (x :: BoxedRep Lifted) -> BoxedRep Lifted =
    eval @(BoxedRep Lifted) (x :: BoxedRep Lifted)
  scope 1 = "" Dependency

  scope 2 = "" Target

  global 2.useFunction = (F$grin_useFunction_thunk_0)

  global 2.useCaf = (F$grin_useCaf_thunk_1)

  $grin_useFunction_thunk_0 -> BoxedRep Lifted =
    function_whnf :: BoxedRep Lifted <-
      eval @(BoxedRep Lifted) global-ref 1.identity
    apply @(BoxedRep Lifted) (function_whnf :: BoxedRep Lifted) global-ref 1.caf

  $grin_useCaf_thunk_1 -> BoxedRep Lifted =
    eval @(BoxedRep Lifted) global-ref 1.caf
extensions:
- MagicHash
modules:
- |
  module Dependency where
  import GHC.Prim
  data Box = Box Int#
  identity :: Box -> Box
  identity value = value
  caf :: Box
  caf = Box 7#
- |
  module Target where
  import GHC.Prim
  import Dependency
  useFunction :: Box
  useFunction = identity caf
  useCaf :: Box
  useCaf = caf
reason: uses the shared FC type environment without dependency program access
status: pass
