extensions: []
source: |
  module Test where
  identity x = x
  unit = identity identity ()
expected: |-
  scope 1 = "" Test

  scope 2 = "aihc-prim" GHC.Tuple

  global 1.identity = (P$identity/1)

  global 1.unit = (F$unit_thunk)

  $identity (x :: BoxedRep Lifted) -> BoxedRep Lifted =
    eval @(BoxedRep Lifted) (x :: BoxedRep Lifted)

  $unit_thunk -> BoxedRep Lifted =
    function_application :: BoxedRep Lifted <-
      call @(BoxedRep Lifted) $identity global-ref 1.identity
    apply @(BoxedRep Lifted) (function_application :: BoxedRep Lifted) global-ref 2."()"
status: pass
reason: applies remaining arguments after a direct local function call reaches its arity
