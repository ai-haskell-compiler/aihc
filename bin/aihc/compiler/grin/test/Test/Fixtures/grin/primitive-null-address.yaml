expected: |-
  scope 1 = "" Test

  scope 2 = "aihc-prim" GHC.Prim

  constructor 1.Address [AddrRep]

  global 1.Address = (C1.Address/1)

  global 1.value = (F$grin_value_thunk_0)

  $grin_value_thunk_0 -> BoxedRep Lifted =
    argument :: AddrRep <-
      eval @AddrRep global-ref 2.nullAddr#
    store (C1.Address (argument :: AddrRep))
extensions:
- MagicHash
reason: evaluates an imported nullary primitive through its global binding
source: |
  module Test where
  import GHC.Prim
  data Address = Address Addr#
  value :: Address
  value = Address nullAddr#
status: pass
