expected: |-
  scope 1 = "" Test

  foreign labs = "labs" :: (int) -> int ! pure

  global 1.labs = (P$grin_labs_foreign_0/[[IntRep]])

  $grin_labs_foreign_0 (foreign_argument_0 :: IntRep) -> IntRep =
    foreign-call labs = "labs" :: (int) -> int ! pure with (foreign_argument_0 :: IntRep)
extensions:
- ForeignFunctionInterface
- MagicHash
source: |
  module Test where
  import GHC.Prim
  foreign import ccall safe "labs" labs :: Int# -> Int#
reason: a safe C call lowers like an unsafe C call
status: pass
