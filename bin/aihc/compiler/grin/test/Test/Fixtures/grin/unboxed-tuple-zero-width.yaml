expected: |-
  scope 1 = "" Test

  constructor 1.Box [IntRep]

  primitive realWorld# :: TupleRep []/0

  global 1.value = (F$value_thunk)

  $value_thunk -> BoxedRep Lifted =
    () <-
      primitive-call @(TupleRep []) realWorld#
    store (C1.Box (42 :: IntRep))
extensions:
- EmptyDataDecls
- MagicHash
- UnboxedTuples
reason: Evaluates State# expressions and omits their runtime values.
source: |
  module Test where
  import GHC.Prim
  data Box = Box Int#
  foreign import prim realWorld# :: State# RealWorld
  value :: Box
  value = Box (case (# realWorld#, 42# #) of (# _, answer #) -> answer)
status: pass
