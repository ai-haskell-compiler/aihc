expected: |-
  constructor Test.Box [IntRep]

  global "\NULTest\NULBox" = (CTest.Box/1)

  global "\NULTest\NULvalue" = (F$grin_value_thunk_0)

  $grin_value_thunk_0 -> BoxedRep Lifted =
    argument :: IntRep <-
      constant (42 :: IntRep)
    store (CTest.Box (argument :: IntRep))
extensions:
- MagicHash
reason: stores a saturated constructor directly and preserves its IntRep field representation
source: |
  module Test where
  import GHC.Prim
  data Box = Box Int#
  value :: Box
  value = Box 42#
status: pass
