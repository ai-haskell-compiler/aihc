; error: @main/entry: add does not accept f64
; error: @main/entry: lt.s does not accept ptr
; error: @main/entry: switch does not accept f64

func @main() -> f64 {
entry:
  %x = add f64 1.0, 2.0
  %c = lt.s ptr null, null
  switch f64 %x {
    default -> done
  }

done:
  return %x
}
