program: |
  $entry (value%1 :: IntRep) -> IntRep =
    case (value%1 :: IntRep) as scrutinee%2 :: IntRep of
      0 ->
        constant (10 :: IntRep)
      1 ->
        constant (20 :: IntRep)
      _ ->
        constant (30 :: IntRep)
expected: |
  extern func @aihc_lir_continue_i(ptr, ptr, ptr, ptr, ptr, ptr, i64)

  func @aihc_f_aihc_entry___sentry(%machine_0: ptr, %hp_1: ptr, %hp_limit_2: ptr, %sp_3: ptr, %sp_limit_4: ptr, %value_5: i64, %$cps_return_6: ptr) {
  entry:
    switch i64 %value_5 {
      0 -> alt_7
      1 -> alt_8
      default -> alt_9
    }

  alt_7:
    tailcall @aihc_lir_continue_i(%machine_0, %hp_1, %hp_limit_2, %sp_3, %sp_limit_4, %$cps_return_6, 10)

  alt_8:
    tailcall @aihc_lir_continue_i(%machine_0, %hp_1, %hp_limit_2, %sp_3, %sp_limit_4, %$cps_return_6, 20)

  alt_9:
    tailcall @aihc_lir_continue_i(%machine_0, %hp_1, %hp_limit_2, %sp_3, %sp_limit_4, %$cps_return_6, 30)
  }
status: pass
reason: |
  A case on an unboxed scalar is one switch with the default alternative as the
  fallback, and allocates nothing, so the function has no reservation at all.
