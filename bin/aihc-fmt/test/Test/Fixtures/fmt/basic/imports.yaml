extensions: []
input: |
  {-# LANGUAGE ImportQualifiedPost #-}
  module Main where
  import Data.Text qualified as T
  import Data.Map (Map)
  x=1
formatters:
  aihc-fmt:
    status: pass
    output: |
      {-# LANGUAGE ImportQualifiedPost #-}
      module Main where
      import Data.Text qualified as T
      import Data.Map (Map)
      x = 1
  ormolu:
    status: pass
    output: |
      {-# LANGUAGE ImportQualifiedPost #-}

      module Main where

      import Data.Map (Map)
      import Data.Text qualified as T

      x = 1
  hindent:
    status: pass
    output: |
      {-# LANGUAGE ImportQualifiedPost #-}

      module Main where

      import Data.Map (Map)
      import Data.Text qualified as T

      x = 1
  stylish-haskell:
    status: pass
    output: |
      {-# LANGUAGE ImportQualifiedPost #-}
      module Main where
      import           Data.Map  (Map)
      import qualified Data.Text as T
      x=1
